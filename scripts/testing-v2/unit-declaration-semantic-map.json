[
  {
    "baseFile": "tests2/core/test-map-execution.test.ts",
    "baseName": "accepts path-derived core, dom, integration, isolated, and approved E2E ownership",
    "current": [
      {
        "file": "tests2/core/test-discovery.test.ts",
        "name": "classifies %s from POSIX and Windows paths"
      },
      {
        "file": "tests2/core/test-discovery.test.ts",
        "name": "recursively discovers, sorts, and assigns every active convention exactly once"
      }
    ],
    "rationale": "The map-backed ownership fixture was replaced by path classification and recursive discovery fixtures. Together they retain every Vitest lane, add the other path-owned lanes, and verify both Windows and POSIX path forms without a registry."
  },
  {
    "baseFile": "tests2/core/test-map-execution.test.ts",
    "baseName": "rejects cross-tagging %s",
    "current": [
      {
        "file": "tests2/core/test-discovery.test.ts",
        "name": "rejects unsupported or ambiguous placement %s actionably"
      },
      {
        "file": "tests2/core/test-discovery.test.ts",
        "name": "rejects managed-root suffix mismatches found during traversal"
      }
    ],
    "rationale": "Execution metadata can no longer be cross-tagged because ownership is derived from path and suffix. The successors reject conflicting semantic suffixes, wrong runner roots, and traversal-time mismatches."
  },
  {
    "baseFile": "tests2/core/test-map-execution.test.ts",
    "baseName": "rejects %s isolated reasons",
    "current": [
      {
        "file": "tests2/core/test-discovery.test.ts",
        "name": "classifies %s from POSIX and Windows paths"
      },
      {
        "file": "tests2/core/test-discovery.test.ts",
        "name": "rejects unsupported or ambiguous placement %s actionably"
      }
    ],
    "rationale": "Free-form isolated reasons were retired with the map. The isolated suffix is now the semantic declaration, and the successors prove its canonical classification and reject ambiguous or unsupported uses."
  },
  {
    "baseFile": "tests2/core/test-map-execution.test.ts",
    "baseName": "rejects an isolated reason on a non-isolated project",
    "current": [
      {
        "file": "tests2/core/test-discovery.test.ts",
        "name": "rejects unsupported or ambiguous placement %s actionably"
      }
    ],
    "rationale": "A non-isolated project can no longer carry isolated metadata. The replacement classifier rejects isolated semantic suffixes outside the supported core and integration roots and rejects multiple semantic suffixes."
  },
  {
    "baseFile": "tests2/core/test-map-execution.test.ts",
    "baseName": "preserves duplicate ownership and missing physical ownership validation",
    "current": [
      {
        "file": "tests2/core/test-discovery.test.ts",
        "name": "recursively discovers, sorts, and assigns every active convention exactly once"
      },
      {
        "file": "tests2/core/test-discovery.test.ts",
        "name": "does not follow symlinked directories"
      },
      {
        "file": "tests2/core/test-discovery.test.ts",
        "name": "rejects managed-root suffix mismatches found during traversal"
      }
    ],
    "rationale": "Filesystem discovery eliminates registry entries for missing files. The successors retain exactly-once ownership, deterministic physical traversal, symlink exclusion, and rejection of test-shaped files that do not have one valid owner."
  },
  {
    "baseFile": "tests2/core/full-test-lane-entrypoints.test.ts",
    "baseName": "keeps retired package and tests-map entrypoints absent",
    "current": [
      {
        "file": "tests2/core/full-test-lane-entrypoints.test.ts",
        "name": "keeps retired package, test, and map-only entrypoints absent"
      }
    ],
    "rationale": "The renamed successor retains the affected-runner package and test absence checks and extends the same assertion to the retired map-only implementation and data files."
  },
  {
    "baseFile": "tests2/core/guard-v2.test.ts",
    "baseName": "has a v2Native allowlist (guard self-coverage)",
    "current": [
      {
        "file": "tests2/core/guard-v2.test.ts",
        "name": "discovers every active test exactly once and covers itself"
      }
    ],
    "rationale": "Convention discovery removes the v2Native allowlist entirely. The successor retains self-coverage by proving the guard is automatically discovered in the core lane."
  },
  {
    "baseFile": "tests2/core/guard-v2.test.ts",
    "baseName": "claims every actual tests2/{core,dom,integration} file (no orphans)",
    "current": [
      {
        "file": "tests2/core/guard-v2.test.ts",
        "name": "discovers every active test exactly once and covers itself"
      }
    ],
    "rationale": "The successor derives all active files from the filesystem, compares every leaf assignment with the complete discovery union, and rejects duplicate or orphaned active tests without registration."
  },
  {
    "baseFile": "tests2/core/guard-v2.test.ts",
    "baseName": "resolves every migrated v2Path to an existing file (no dangling references)",
    "current": [
      {
        "file": "tests2/core/guard-v2.test.ts",
        "name": "discovers every active test exactly once and covers itself"
      },
      {
        "file": "tests2/core/full-test-lane-entrypoints.test.ts",
        "name": "keeps retired package, test, and map-only entrypoints absent"
      }
    ],
    "rationale": "There are no v2Path references after registry retirement. Physical discovery now owns existence, while the entrypoint guard proves the obsolete registry and map-only consumers stay absent."
  },
  {
    "baseFile": "tests2/core/guard-v2.test.ts",
    "baseName": "resolves every v2Native path to an existing file",
    "current": [
      {
        "file": "tests2/core/guard-v2.test.ts",
        "name": "discovers every active test exactly once and covers itself"
      },
      {
        "file": "tests2/core/full-test-lane-entrypoints.test.ts",
        "name": "keeps retired package, test, and map-only entrypoints absent"
      }
    ],
    "rationale": "There are no v2Native references after registry retirement. Filesystem discovery provides physical existence and self-coverage directly, and the entrypoint guard prevents the retired allowlist from returning."
  },
  {
    "baseFile": "tests2/core/guard-v2.test.ts",
    "baseName": "keeps migrated flag and v2Path consistent",
    "current": [
      {
        "file": "tests2/core/guard-v2.test.ts",
        "name": "uses NUL-safe add-only Git path admission so rename destinations cannot hide"
      },
      {
        "file": "tests2/core/guard-v2.test.ts",
        "name": "admits semantic rename destinations without changing execution discovery"
      },
      {
        "file": "tests2/core/full-test-lane-entrypoints.test.ts",
        "name": "keeps retired package, test, and map-only entrypoints absent"
      }
    ],
    "rationale": "The migrated flag and v2Path state were deleted rather than translated. The successors keep that registry absent and validate every introduced or renamed destination independently of execution discovery, including NUL-safe Git path handling."
  }
]
