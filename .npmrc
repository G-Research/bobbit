# DO NOT REMOVE without reading this.
#
# `@earendil-works/pi-coding-agent@0.82.1` publishes an npm-shrinkwrap.json with
# protobufjs@7.6.5, but the dependency-owned lock also pins brace-expansion@5.0.7
# (high GHSA-mh99-v99m-4gvg). Bobbit cannot repair that immutable upstream tree
# with root overrides. A clean root audit is therefore not evidence about the
# packed consumer. Keep the non-zero packed-consumer result visible. For this
# 0.82.1 delivery, a human explicitly accepted this exact immutable finding, so
# it does not block merge or release eligibility; the result is not audit-clean
# and the exception does not cover any additional finding or audit failure.
#
# On npm >=10 the legacy `shrinkwrap` key maps to `package-lock`, so this line
# means package-lock=false: it FREEZES the committed lockfile so a stray
# `npm install` cannot regenerate it from dependency-owned shrinkwraps.
# `npm ci` still installs from the committed lockfile as normal. Neither the
# committed lock nor a clean root `npm audit` proves the packed consumer secure.
#
# For controlled dependency/lock regeneration:
# 1. Stop gateway/vite/native-module holders. Back up this file outside the repo
#    with a failure trap, then temporarily move it out of the repository.
# 2. Delete the OLD installed
#    node_modules/@earendil-works/pi-coding-agent/npm-shrinkwrap.json.
# 3. Run `npm install --package-lock=true` while this file is out of the repo.
# 4. Restore ONLY this .npmrc; never restore the deleted old shrinkwrap.
# 5. npm 10 and 11 strip better-sqlite3's published `gypfile: false` from
#    regenerated lock metadata. Re-add the exact boolean field at
#    packages["node_modules/better-sqlite3"].gypfile and parse-check it is false;
#    without it `npm ci` synthesizes an unwanted node-gyp rebuild.
# 6. Verify npm freshly extracted the selected-version coding-agent shrinkwrap,
#    all three Pi packages are aligned exactly at 0.82.1, every protobufjs is
#    7.6.5+, and root/non-shrinkwrapped ws is 8.21+; keep the shrinkwrap-owned
#    brace-expansion@5.0.7 high advisory visible in audit reporting.
# 7. Pack and install Bobbit in an empty project, then audit that consumer.
#    Record the exact non-zero brace-expansion result for the approved 0.82.1
#    exception. Any changed or additional finding, or unavailable audit service,
#    remains blocking unless separately reviewed and explicitly accepted.
shrinkwrap=false
