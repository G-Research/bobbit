id: terminal.session-menu
kind: session-menu
label: Open terminal
icon: terminal
target:
  action: channel-panel
  channel: terminal
  singletonKey: session-terminal
  panelId: terminal.panel
  params:
    autoStart: true
