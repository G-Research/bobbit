name: graph_status
group: Graph
summary: Read graph freshness, lifecycle availability, versions, and measurements.
description: Read-only Graphify freshness; stale or base-fallback status cannot prove current impact.
params: [component, components]
provider:
  type: bobbit-extension
  extension: extension.ts
examples:
  - graph_status
notes:
  - Status never returns filesystem paths or raw graph artifacts.
