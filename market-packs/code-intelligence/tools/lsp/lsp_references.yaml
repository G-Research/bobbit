name: lsp_references
description: "Read-only precise reference navigation through a ready LSP; read cited source and callers before review findings."
summary: "Read-only LSP reference lookup"
params: ["component?", "language?", path, position]
provider:
  type: bobbit-extension
  extension: extension.ts
group: Code Intelligence
docs: |-
  Finds references only through an enabled, ready LSP service rooted at the
  current linked-worktree component. It never starts a server or falls back to
  structural search.
