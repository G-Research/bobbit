id: experiment-runner.palette
kind: command-palette
label: New experiment
# A command-palette LAUNCHER. Its selection IS the user gesture. Opens the
# experiment panel in the current session at mode-select (A/B default).
target:
  panelId: experiment-runner.panel
