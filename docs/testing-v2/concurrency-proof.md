# Concurrency Proof Report

**Status:** ⏳ Option A (multi-worktree) IMPLEMENTED in `concurrency-proof.mjs`; awaiting a chaos-free window for the final 5×3 run (which regenerates this file with real numbers). The ledger cap is PROVEN. The "5 runs from one worktree" model was rejected as a harness artifact — see "Findings from the real run" below.

> This file is regenerated by `node scripts/testing-v2/concurrency-proof.mjs`. The two real
> 5×3 attempts on a chaos-free machine are recorded here honestly, including WHY they failed
> (harness artifact, not a ledger or product flake) and the open decision needed to finish.

## What is PROVEN

### Ledger caps Σworkers ≤ cores (the D7 mechanism) ✅

Both real 5×3 attempts passed **A3 (ledger Σworkers ≤ cores)** and **A4 (per-run wall ≤ cap)**:

- Under 5 concurrent `test:v2` runs the ledger granted each run vitest 2 + playwright 2 = 4
  slots, **peak Σ = 20/24** (attempt 2) / 24/24 (attempt 1 momentary), never exceeding 24.
- Verified independently by `node scripts/testing-v2/ledger.mjs --stress` (near-simultaneous,
  staggered, 8-way backpressure, and SIGKILL crash-recovery — Σ ≤ cores in every case) and
  `--selftest` (pending-contention cap + splitBundle sum-preservation).

The pending-marker fix (count peers still inside the coalescing window) plus the sum-preserving
`splitBundle` (removing the "+3 forced-grant overshoot") is the CPU-exhaustion fix D7 asked for
and it works.

### Reproducible install ✅

`package-lock.json` regenerated for vitest/happy-dom/memfs (root cause: `.npmrc` `shrinkwrap=false`
= npm `package-lock=false`, so plain `npm install` never wrote the lock). `npm ci` now succeeds
(675 packages, `.bin` populated) — required for the switchover gate.

## Findings from the real run (why A1/A2 failed — NOT the ledger)

Running **5 concurrent `test:v2` from a single worktree** corrupts state shared inside that one
`node_modules`, which has nothing to do with the ledger:

1. **`node_modules/.bin` wiped mid-run.** With `package-lock=false`, npm treats `node_modules`
   as out-of-sync and *reifies* (`.bin` remove→recreate) on `npm run`; 5 concurrent reifies race
   and leave `.bin` empty → reps 2 & 3 died instantly with `'vitest' is not recognized`.
   *Mitigated* by a canonical `npm ci` (now that the lock is correct).
2. **Vite dep-optimizer cache corruption.** All 5 vitest processes share
   `node_modules/.vite`; concurrent clear/rewrite produced spurious module-load failures
   (`TypeError: RemoteAgent is not a constructor` — a lazy import resolving to `undefined`),
   255 "failed" files in attempt 1.
3. **Isolating the Vite cache per run trades corruption for a CPU storm.** Giving each run its
   own cold `cacheDir` made all 5 optimize deps simultaneously → each run ballooned to ~780 s
   (> the 600 s cap) and vitest still failed under the contention. A single `test:v2:core`
   with 2 forks + isolated cache passes cleanly (exit 0) — the failure is specifically the
   5-way same-worktree contention, not the tests.

**Conclusion:** "5 runs from one worktree" is not a realistic concurrency model. Real N-developer
concurrency = **N separate worktrees**, each with its own `node_modules` (warm Vite cache, no
`.bin` race) and harness-isolated ports — exactly what the cross-process ledger was built to
coordinate. The proof harness should spawn each concurrent `test:v2` from its own checkout.

## Decision needed (blocking the final green proof)

- **Option A (recommended): multi-worktree proof.** `concurrency-proof.mjs` provisions N throwaway
  worktrees (`git worktree add` + `npm ci` once each, cached between reps), runs one `test:v2` per
  worktree concurrently, and asserts the same A1–A4. Models reality; removes all shared-`node_modules`
  artifacts; the ledger still coordinates Σ ≤ cores across the worktrees.
- **Option B: keep single-worktree but pre-warm + share the Vite cache read-only** and accept the
  residual contention risk (does not model reality; the `.bin`/port issues remain fragile).

Attempt data for the record:

| Attempt | Model | A1 | A2 | A3 (Σ≤24) | A4 (wall≤600s) | Root cause of A1/A2 |
|---|---|---|---|---|---|---|
| 1 | single worktree, shared cache | ❌ | ❌ | ✅ (peak 24) | ✅ | `.bin` wipe (reify race) + `.vite` corruption |
| 2 | single worktree, isolated cache | ❌ | ❌ | ✅ (peak 20) | ❌ (~780s) | 5× cold-cache CPU storm + contention |

## Reproduce

```bash
npm ci && npm run build         # canonical node_modules + dist
node scripts/testing-v2/ledger.mjs --stress 5 0 6000   # proves Σ≤cores (chaos-independent)
node scripts/testing-v2/concurrency-proof.mjs          # full 5×3 (pending the model decision above)
```
