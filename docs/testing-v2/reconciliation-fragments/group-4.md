# Reconciliation fragment: group 4 — verification ownership and ledger isolation

## Scope and method

- **Audited head:** `0299fe6b7d5a8c529531b334f5a2392f1f31cd8f` (`origin/main` and the parent goal head at audit time).
- **Reference:** `e3051de63cf611143a989f7928bd9f9a7ed9beae`, compared exactly with second parent `f747186a886b0e2e6451d59c246f582edc2e4377`.
- **Focused commits inspected:** #1071 `7a42e234`, #1076 `311b8ec7`, #1077 `aaae5330`, #1078 `91a6bec7`, #1080 `652666e2`, and #1089 `0f5b3a7e`.

The reference changes in this group split into exact process-ownership/restart recovery work and one test-runtime ledger isolation assertion. The current head retains the intended ownership contract through #1089, often with stronger state/attestation boundaries. No category-4 candidate was found.

| Reference file | Residual behavior | Category | Current-head evidence and disposition |
|---|---|---:|---|
| `src/server/agent/verification-command-runner.ts` | Carry a nonce-bound POSIX sentinel identity from `VerificationCommandSpawnSpec` into `spawnTracked`, so a durable command’s recovered process group can be proven before cleanup. | 1 | **Merged by #1089.** The current `posixSentinelIdentity` spec field and `realVerificationCommandRunner.spawn()` forwarding retain this contract. `VerificationHarness` supplies the record for detached host and durable `docker exec` launches at `verification-harness.ts:6686,6719`. |
| `src/server/agent/verification-logic.ts` | Refuse host-detached restart recovery on every Windows host, not only when Git Bash is absent; a persisted numeric PID cannot atomically prove the original tree. Return retryable/pending rather than inventing a verdict. | 1 | **Merged by #1089.** `supportsHostDetachedCommandRecovery()` and `decideCommandRecoveryMode()` at `verification-logic.ts:204-217` make all `win32` host commands `pending-retry`; container commands retain their separate recovery mode. |
| `src/server/agent/verification-harness.ts` | Represent durable sentinel identity and pending sentinel cleanup; inject platform, lifecycle clock, PID identity inspection, killer, and reaper seams to test failure paths deterministically. | 1 | **Merged by #1089.** Current `ActiveVerification` has `sentinelFile`/`sentinelCleanupPending`; construction records injectable seams at `verification-harness.ts:3416-3454`. `_reapRecoveredPosixSentinel()` at `3721` fails closed when identity evidence is absent, stale, mismatched, or reused. |
| `src/server/agent/verification-harness.ts` | On startup, do not drop a persisted verification merely because its goal is complete/shelved: mark cancellation, clean its exact command state, and retain/schedule it when cleanup cannot yet be proved. | 1 | **Merged by #1089.** The recovered-goal branch at `2504-2515` calls `_killPersistedCommandSteps()` and finalizes only after settlement; failed/pending ownership cleanup is retained for retry rather than deleted. |
| `src/server/agent/verification-harness.ts` | For recovered host/container steps, signal only an identity-verified group; never retarget a historical PID/PGID. Reap container payload first, then the exact host `docker exec` transport, recording each phase durably. | 1 | **Merged by #1089.** `_killPersistedCommandSteps()` documents and implements payload-before-transport ordering at `3956-4007`; `_killAndVerifyRecoveredContainerProcessGroup()` at `7651` performs exact live-group verification. Current code adds durable payload/transport completion fields and witness checks beyond the reference, preserving the reference invariant more strongly. |
| `src/server/agent/verification-harness.ts` | Set up host and container sentinel records before durable launch; keep the container PID file after live timeout cleanup; make the container leader a session/group leader; attach the host transport sentinel. | 1 | **Merged by #1089.** Current durable setup creates host records at `6406-6457`, supplies `posixSentinelIdentity` at `6686`, and records container ownership readiness/completion state. The expanded implementation separates payload and transport witnesses rather than relying on the reference’s PID-file-only proof. |
| `src/server/agent/verification-harness.ts` | Do not report a command timeout, cancellation, or zero exit as safe until `TrackedChild.waitForTreeExit()` proves completion. Do not issue a late numeric-PID kill after root exit/exit-file observation; use an injectable clock for exit-without-close regression coverage. | 1 | **Merged by #1089.** `commandLifecycleClock` is used by command lifecycle settlement; timeout/cancel output at `7089-7105` distinguishes verified cleanup from unverified cleanup; natural completion fails closed at `7111`. Current cancellation also waits for ownership readiness before publication. |
| `src/server/agent/verification-harness.ts` | Before finalizing a recovered host exit file or container result, reap the proven sentinel. For durable containers, prove/reap the in-container payload and host transport for successful, timed-out, and no-verdict outcomes. | 1 | **Merged by #1089.** Recovered host finalization invokes `_reapRecoveredPosixSentinel()` at `7376-7379`; container transport cleanup is persisted and completed at `7417-7421` and `7731-7739`. The current transport-completion state prevents a crash between payload and host-transport cleanup from finalizing early. |
| `tests2/core/verification-command-restart-lifecycle.test.ts` | Pin Windows host PID recovery as unsupported; pin payload-before-transport ordering for cancellation/final recovered container verdict; ensure Windows cannot treat a POSIX-reaper no-op as payload proof. | 1 | **Merged by #1089.** Current tests retain the Windows host failure test (`164`), cancellation ordering (`255-278`), crash-after-host-result recovery (`281-306`), and Windows container coverage (`349`). They additionally pin exact container witness/transport retry behavior (`309-347`). |
| `tests2/core/verification-harness-timeout.test.ts` | Pin failure when tree cleanup/completion cannot be verified, including zero exits and Windows exit-with-open-descendant; advance a manual lifecycle clock rather than sleep. | 1 | **Merged by #1089.** Current tests at `438-472` retain all three fail-closed cases. They add ownership-readiness cancellation, durable transport persistence, and concurrent child cleanup coverage (`475-677`), strengthening the same lifecycle boundary. |
| `tests2/harness/fake-verification-command-runner.ts` | Supply fake `ownershipReady` and `waitForTreeExit` implementations required by the expanded tracked-child contract. | 2 | **Superseded by newer #1089 behavior.** The fake retains immediate ownership readiness, but `waitForTreeExit()` now returns `closed` (`208-209`) instead of unconditional success. This makes the fake obey the same completion boundary and prevents premature successful cleanup claims. |
| `tests2/integration/_e2e/fake-cmd-setup.ts` | Supply the same tracked-child ownership/completion contract to the manual integration fake. | 2 | **Superseded by newer #1089 behavior.** The manual fake now returns `closed` from `waitForTreeExit()` (`106-107`) rather than unconditional success, preserving deterministic manual-clock tests while accurately modeling lifecycle state. |
| `tests2/core/ledger-lease-bridge-interop.test.ts` | Assert the in-process bridge/ledger interop test begins from its isolated ledger directory and an empty lease set, never the machine-global browser lease pool. | 2 | **Superseded by #1071’s stronger explicit-root, two-process contract.** `beforeAll` sets `BOBBIT_V2_LEDGER_DIR` to a per-file `mkdtemp` root (`155-160`), while `shares an explicit ledger root across processes with distinct temp roots` (`170-183`) proves two coordinators with distinct `TEMP`/`TMP`/`TMPDIR` values observe the same explicit root and both reservations. This subsumes the reference’s local-directory/empty-start assertions without reintroducing host-temp spelling dependence. |

## Category summary

- **1 — merged by focused PR:** 10 behaviors, all retained by #1089 exact-process-ownership recovery (with the ledger’s original isolation foundation in #1071).
- **2 — superseded by newer upstream behavior:** 3 behaviors: both fakes now model actual close state, and the ledger test uses the stronger #1071 explicit-root/two-process proof.
- **3 — intentionally obsolete/inapplicable:** none.
- **4 — genuinely missing:** none; no implementation is requested from this fragment.
