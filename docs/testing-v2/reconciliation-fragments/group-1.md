# Residual reconciliation — group 1

**Audit head:** `0299fe6b8268f01f136d2a6787983e662e0fdc94` (`origin/main` at audit time)<br>
**Reference extraction:** `git diff e3051de63cf611143a989f7928bd9f9a7ed9beae^2 e3051de63cf611143a989f7928bd9f9a7ed9beae`<br>
**Scope:** residual entries 1–9: `.gitignore`, `fast-gate-design.md`, packed-consumer audit, workflow editor, Pi discovery, project/cwd identity, session steer recovery, and spawn-tree ownership.

Categories: **1** merged by a focused PR; **2** superseded by newer upstream behavior; **3** intentionally obsolete/inapplicable; **4** missing from the audit head. Category 4 entries are candidates only; this fragment implements nothing.

| Residual file and distinct behavior | Category | Evidence / disposition |
|---|---:|---|
| `.gitignore`: ignore UUID-suffixed checkout-local `test-results-v2*` artifacts. | 3 | The later coordinator contract puts Playwright artifacts under the run-owned system-temp root (`scripts/testing-v2/run-browser-v2.mjs:39-40`; `playwright-v2.config.ts:196`). Current `.gitignore` retains the former fixed `test-results-v2/` guard for legacy/manual output, so broad checkout-local matching is deliberately no longer the ownership mechanism. The historical implementation is `27fd571c`, which is not an ancestor of the audit head. |
| `docs/testing-v2/fast-gate-design.md`: describe retries as diagnostic debt and require retry-free qualification. | 4 | The underlying invariant is not true for the unit gate at the audit head: `vitest.config.ts:51` hard-codes `retry: 3`, and neither `package.json` nor the unit wrapper consumes `BOBBIT_V2_RETRY_FREE`; that variable is only consumed by `scripts/testing-v2/run-e2e-v2.mjs:312,350,366`. The residual text says CLI `--retry=0`, while the approved goal contract requires `BOBBIT_V2_RETRY_FREE=1` for all suites. Add a unit retry-free implementation and regression pin before updating frozen design prose. |
| `scripts/release-packed-consumer-audit.mjs`: create the packed-consumer audit directory as an owned child of `BOBBIT_V2_RUN_ROOT`, canonicalized with `realpathSync`. | 4 | Current `runPackedConsumerAudit()` still calls `mkdtemp(join(tmpdir(), "bobbit-release-packed-audit-"))` at line 449 and exports no `packedConsumerTempPrefix`. Historical commit `2a8db4a5` introduced the bounded run-root child and is not in `origin/main`. This is a concrete coordinator-artifact isolation gap; add a focused test of fallback, run-root child, and cleanup ownership. |
| `src/app/workflow-page.ts`: revision-aware save response must preserve edits made while a create/PUT is in flight. | 1 | Focused PR `#1076` (`311b8ec7`) implements the stronger owner-generation plus revision protocol. It is pinned by `tests2/browser/fixtures/goal-workflow-editor.spec.ts` tests for held create, held PUT with omitted refresh target, and navigation to another draft (lines 218–321). |
| `src/app/workflow-page.ts`: do not rerender a verification-step text control merely because failed-save validation is active; it can clobber an intermediate input and collapse Advanced. | 4 | The audit head still has `if (rerender || saveAttempted) renderApp()` at line 975. Residual commit `9a94eb19` changes this to structural-rerender-only and adds the focused timeout editor regression in `tests2/browser/workflow-review-timeout-editor.spec.ts`; it is not an ancestor of `origin/main`. Preserve the #1076 owner-generation behavior when applying this candidate. |
| `src/server/agent/pi-extension-contributions.ts`: load async Pi discovery in the initial module graph rather than lazy-importing after an isolated filesystem/prebundle fixture is installed. | 4 | Current line 345 retains `await import("./pi-extension-discovery.js")`; the residual eager import is historical commit `6216bbfb`, not in `origin/main`. This is a concrete module-cache/fixture-order candidate; reproduce through the prebundle fixture before implementation and add its regression pin. |
| `src/server/agent/project-registry.ts`: dialect-aware canonical identity, bounded case-semantics evidence, symlink/separator alias dedupe, and dialect-safe containment. | 2 | Focused PR `#1077` (`aaae5330`) is authoritative and substantially stronger than the residual version: `PathIdentityStat` preserves `isDirectory`, cache records bind both fingerprint and directory identity, same-inode file hard links remain inconclusive, Win32 roots are normalized without collapsing unproven descendants, and `findByCwd` uses slash-normalized POSIX-relative containment. Pins: `tests2/core/project-registry-root-paths.test.ts` (foreign Win32, case-sensitive descendants, APFS, hard links, cache replacement). Do not restore the residual's less-conservative folding/relative implementation. |
| `src/server/agent/resolve-project.ts`: canonicalize the longest existing cwd prefix and use `path.relative` boundary checks rather than string-prefix containment. | 1 | This exact behavior landed in focused PR `#1077` (`aaae5330`); audit-head symbols are `realOrResolved()` and `isSameOrDescendant()` at lines 91–119. The path-identity suite above is its regression coverage. |
| `src/server/agent/session-manager.ts`: recovery after Stop must not redispatch an already echoed steer. | 2 | Focused PR `#1076` (`311b8ec7`) deliberately treats the user-role echo as the settlement boundary. The residual reverses that behavior for an echo/Stop race. Audit-head pins explicitly require no replay of echoed work and recovery only for later unechoed work: `tests2/core/session-manager-direct-prompt-lifecycle.test.ts:852–918`, plus restart-once coverage in `tests2/integration/steer-gateway-restart.test.ts`. |
| `src/server/agent/session-manager.ts`: preserve persisted lexical agent-session-file spelling after validation instead of returning the canonicalized sanitizer path. | 2 | #1077's canonical path contract is authoritative. The current return uses the validated `safePath`; restoring the raw persisted spelling would let lexical aliases leak back into lifecycle identity. `aaae5330` and `tests2/core/transcript-sanitizer.test.ts` are the path/sanitizer evidence. |
| `src/server/agent/spawn-tree.ts`: retain sentinel/container handoff state, exact POSIX identity proof, deferred timeout arming, and Windows completion/control plumbing from the historical branch. | 2 | Focused PR `#1089` (`0f5b3a7e`) replaced this intermediate design with exact process-ownership recovery and is authoritative. Its current `spawn-tree.ts` interfaces and lifecycle paths are pinned by `tests2/core/spawn-tree-process-cleanup.test.ts` (including no PID retarget after root exit, POSIX/Windows readiness, and container proof) and the native ownership E2E suite. The residual removes those stronger authority boundaries; do not restore it. |

## Result

- **Category 1:** workflow save revision protocol; cwd containment/canonical existing-prefix resolution.
- **Category 2:** project identity refinements; echoed-steer and persisted-path semantics; process-tree ownership design.
- **Category 3:** checkout-local Playwright ignore wildcard.
- **Category 4 candidates:** unit retry-free control, packed-consumer run-root isolation, validation-time input stability, and eager Pi discovery loading.

No tests or assertions were weakened, removed, or changed by this audit.
