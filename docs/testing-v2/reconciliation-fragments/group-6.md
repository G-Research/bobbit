# Reconciliation fragment — group 6

**Reference:** `e3051de63cf611143a989f7928bd9f9a7ed9beae^2..e3051de63cf611143a989f7928bd9f9a7ed9beae`  
**Audited head:** `0299fe6b8268f01f136d2a6787983e662e0fdc94` (`origin/main`)  
**Focused history checked:** #1071 `7a42e234`, #1076 `311b8ec7`, #1077 `aaae5330`, #1078 `91a6bec7`, #1080 `652666e2`, and #1089 `0f5b3a7e`.

Categories: **1** merged; **2** superseded by current upstream; **3** intentionally obsolete/inapplicable; **4** genuinely missing. Category 4 entries below are audit findings only; this fragment intentionally makes no implementation change.

| Reference file and distinct behavior | Category | Evidence and reconciliation |
|---|---:|---|
| `src/server/agent/session-manager.ts` — a user-role echo settles its in-flight steer; Stop requeues only unechoed records, in dispatch order, ahead of ordinary queued work. | **1** | Merged by #1076 (`311b8ec7`). `SessionManager._consumeSteerEcho()` and `_reconcileInFlightSteers()` retain the durable settlement and reverse-front insertion contract. `tests2/core/session-manager-direct-prompt-lifecycle.test.ts` contains the exact residual assertions for echoed, later-unechoed, multi-steer ordering, and ordinary-queue priority. |
| `src/server/agent/session-manager.ts` — do not dispatch queued steers through the live RPC from the terminal `agent_end` of an abort; reconcile and drain through the post-abort path exactly once. | **1** | Merged by #1076 (`311b8ec7`): the non-aborting safety-net dispatch is in the `else` branch at `handleAgentLifecycle()` while the abort branch calls `_reconcileAfterAbort()`. The exact PI-25b integration regression is present at `tests2/integration/abort-status-e2e.test.ts`. |
| `src/server/agent/session-manager.ts` — return the lexical persisted host transcript spelling after validation rather than the readable/canonical result. | **2** | The unmerged residual commit `e3671973` (`fix: preserve lexical recovery paths`) is superseded by current path-identity authority (#1077). Current `recoverSessionFile()` returns `safePersistedHostAgentSessionFile()` after `trustPersistedAgentSessionFile()`; that helper delegates to `resolveReadablePersistedAgentSessionFile()`. This keeps recovery on the validated readable identity used by the current canonical-path system, rather than reintroducing a second lexical-path authority. |
| `src/server/server.ts` — route sandbox Docker availability, image build/version, status, and sandbox-session validation through the injected gateway command runner so tier-1 fences host process spawning. | **4** | The residual implementation is from unmerged `beebcd69` (`Fix sandbox command runner injection`). Current `origin/main` calls `checkDockerAvailability(...)`, `buildSandboxImage(...)`, and `ensureImageAgentVersion(...)` without the injected runner in the bootstrap and routes; `git blame` attributes those calls to pre-residual `68c408ca`. No current regression pin covers these Docker status/build route seams. This can allow host Docker probing despite a fenced test runner, so it remains a concrete candidate. |
| `tests/e2e/README.md` — document the API project’s two-worker limit rather than four workers. | **4** | Current `playwright-e2e.config.ts` sets the `api` project to `workers: 2` (with Windows contention rationale), while the README still states four. The residual `c6f4222c` documentation correction is absent from `origin/main`. |
| `tests/e2e/README.md` — describe retries as a workflow safety net and require retry-free qualification, rather than treating retried concurrent runs as acceptable proof. | **4** | Current executable policy is authoritative and already supports `BOBBIT_V2_RETRY_FREE=1`: `playwright-e2e.config.ts:67-72` changes retries to zero and states it is the only first-attempt-stability evidence; `playwright-v2.config.ts:147-149` does the same. The README retains the obsolete “deliberate margin” section and its evidence that accepts retried runs. The residual’s conceptual documentation fix is missing; its literal `--retry=0` example must be adapted to the current environment-variable contract. |
| `tests2/core/session-manager-direct-prompt-lifecycle.test.ts` — pin echoed-steer non-replay, later-unechoed recovery, chronological multiple recovery, and priority over ordinary queued prompts. | **1** | Merged byte-for-byte by #1076 (`311b8ec7`), at the four tests beginning `does not replay an echoed live steer after Stop`. |
| `tests2/integration/abort-status-e2e.test.ts` — make PI-25b deterministically suppress the original steer echo and prove exactly one post-abort recovered user turn and a completing agent turn. | **1** | Merged byte-for-byte by #1076 (`311b8ec7`). The test sets `MOCK_STEER_QUEUE_DROP = "always"`, establishes the abort `agent_end` boundary, and asserts one recovered `S_DIRECT` message. |
| `tests2/integration/helpers/local-mock-agent-clock.ts` — attach a session-local mock clock at the restore queue-drain boundary so the first recovered sleep cannot escape to real time. | **1** | Merged byte-for-byte by #1076 (`311b8ec7`) as `restoreWithLocalMockAgentClock()`. It temporarily wraps `drainQueue`, attaches before the matching session drains, restores the method, and fails if no recovered prompt drained. |
| `tests2/integration/steer-gateway-restart.test.ts` — use the restore-boundary clock helper for both user and agent steers, proving one post-restart redispatch without a real-time race. | **1** | Merged byte-for-byte by #1076 (`311b8ec7`): both restart tests call `restoreWithLocalMockAgentClock()` before settling the current prompt. |
| `tests2/integration/skill-surface-consistency.test.ts` — create each P/Q/R fixture root and external custom-skill directory atomically with `mkdtempSync`, not a PID-and-time-derived shared tmp pathname. | **4** | The residual is unmerged `84dddf02` (`test: isolate concurrent workflow runs`). Current setup still derives four names from `process.pid-Date.now()` and calls recursive `mkdirSync`; it has no atomic ownership claim. The retained test has no pin for unique, run-owned roots, so this remains a concrete same-host artifact-isolation candidate. |

## Category 4 handoff

1. Inject the gateway command runner into every sandbox Docker probe/build path and add a focused fenced-runner regression.
2. Reconcile `tests/e2e/README.md` with the live two-worker API and `BOBBIT_V2_RETRY_FREE=1` qualification contracts.
3. Replace skill-surface fixture pathname derivation with atomic run-owned temp roots and pin the ownership behavior.

No assertions or tests were weakened, removed, skipped, or changed by this audit.
