# Reconciliation fragment — group 3

**Reference:** `e3051de63cf611143a989f7928bd9f9a7ed9beae^2..e3051de63cf611143a989f7928bd9f9a7ed9beae`

**Audited exact head:** `0299fe6b8268f01f136d2a6787983e662e0fdc94` (`origin/main`)

**Scope:** `spawn-tree`, worktree inventory/pool lifecycle, and their assigned regression coverage.

Category meanings: **1** merged by a focused PR; **2** superseded by newer authoritative upstream behavior; **3** intentionally obsolete/inapplicable; **4** genuinely missing (candidate only; not implemented in this audit).

| File | Residual behavior | Category | Evidence and disposition |
|---|---|---:|---|
| `src/server/agent/spawn-tree.ts` | POSIX tracked processes get a same-group sentinel, a pre-exec FD-3 ownership acknowledgement, root-exit finalization, and no post-boundary negative-PID retargeting. | 1 | Current `spawnTracked`, `finishPosixAtRootExit`, `killTree`, and `waitForTreeExit` retain the contract. Focused PR **#1089**, `0f5b3a7e`, contains the file and is an ancestor of the audited head. `tests2/core/spawn-tree-process-cleanup.test.ts` pins natural, timeout, cancellation, fast-exit, readiness-failure, and PID-reuse cases. |
| `src/server/agent/spawn-tree.ts` | Windows starts the payload suspended inside a `KILL_ON_JOB_CLOSE` Job, acknowledges assignment before resume, and never falls back to `taskkill`/a persisted PID. | 1 | Current `WINDOWS_JOB_SUPERVISOR`, `createWindowsJobReadiness`, `ownershipReady`, and `killTreeByPid` provide this behavior. #1089 additionally uses a private shutdown pipe and nonce-bound completion proof, so the reference supervisor is retained with stronger authority boundaries. |
| `src/server/agent/spawn-tree.ts` | Shutdown survival is conditional on established platform ownership; failed readiness is reaped rather than preserved. | 1 | Current `markSurvival`, `killAllTracked`, and the readiness failure paths fail closed. The current core and node-test coverage assert both pre-ready reaping and post-ready preservation. #1089 is the focused merge evidence. |
| `src/server/agent/spawn-tree.ts` | Persisted sentinel identity used a `ps lstart` record alone. | 2 | Current `inspectPosixSentinelIdentity` uses Linux `/proc/<pid>/stat` field 22; Darwin requires `lstart` plus a process-held nonce. `POSIX_TREE_SENTINEL_CHILD_SCRIPT` persists `startTokenKind` and `startToken`. This is a stricter #1089 replacement for the reference record and prevents same-second PID reuse. |
| `src/server/agent/worktree-inventory.ts` | Inventory discovery is async and path identity is lexical at normalization time; async filesystem/Git phases establish existence/identity. | 1 | The reference comment is present verbatim above `norm`; current `scan`, `addFilesystemCandidates`, async `exists`, `opendir`, and `execGit` implement it. The underlying async inventory was merged in `189b83f0` and current path normalization is authoritative after focused PR **#1077** (`aaae5330`). |
| `tests/e2e/pool-claim-restart-resume.spec.ts` | The restart-pool fixture must use a unique, canonical `mkdtemp` root and explicitly delete session, project, and owned root after the assertion. | 4 | The reference change is `4272b489`/`fafad0c3` (`test: isolate pool restart E2E fixture`), neither an ancestor of `origin/main`. Current code uses `join(tmpdir(), \`bobbit-e2e-pool-restart-${Date.now()}\`)`, has suite-level mutable state, and has no cleanup. Concurrent coordinators can collide or leave project/worktree artifacts. **Candidate:** restore a per-test canonical `mkdtemp` root and awaited owned cleanup without weakening the byte-stable branch/reflog/inode assertions. |
| `tests/spawn-tree-shutdown-survival.test.ts` | `markSurvival` remains public; shutdown kills non-survival or pre-ready children, preserves only ownership-ready children, and native Windows Job closure reaps a SIGTERM-ignoring descendant. | 1 | The reference test content is retained at the audited head. It exercises current `ownershipReady`, `killAllTracked`, and `markSurvival`; #1089 is the focused implementation/coverage merge. |
| `tests2/core/purge-preview-pool-shutdown-coder61c7.test.ts` | Lifecycle regression tests must use causal deferred notifications rather than polling/setImmediate turns to observe purge ordering, preview cleanup ordering, pool-concurrency startup, and diagnostics shutdown. | 4 | The reference replaced the `waitFor` polling helper and five scheduling turns with `storePurgeStarted`, `listenerStarted`, `earlierPurgeStarted`, `activeStarted`, `mountCleanupFinished`, and `firstCandidateBatchStarted` deferred barriers. Current head has restored `waitFor` (up to 1,000 `setImmediate` retries) plus three unbounded scheduling turns. This violates the retry-free, event-driven qualification contract. **Candidate:** restore one-shot deferred causal barriers while retaining every existing destructive-owner, no-recreation, ceiling, and shutdown assertion. |
| `tests2/core/spawn-tree-process-cleanup.test.ts` | Native POSIX descendant reaping; exact recovered sentinel cleanup; pre-ready survival rejection; Windows Job readiness/closure; root-exit PID/PGID reuse barriers; and no fallback persisted Windows PID kill. | 1 | This file exists at the audited head and was created by #1089. Current coverage is broader than the reference: it adds nonce-bound Windows completion, exact Darwin nonce tests, Docker transport/payload ownership witnesses, and cancellation-publication cases. The reference assertions are retained or strengthened by the current test helpers and cases. |
| `tests2/core/team-manager.test.ts` | Registered real-Git worktrees must compare through a realpath-aware alias helper, with lexical fallback for missing/stale registrations; a symlink/junction alias is a regression case. | 4 | The reference adds `canonicalWorktreePath` and a symlink/junction assertion. Current `assertRegisteredWorktree` compares only `path.resolve` spellings and the alias test is absent. Focused PR #1077 canonicalized runtime path identity (`normalizeWorktreeHostPath`, `worktree-sweeper`, `worktree-pool`, and `worktree-inventory`), but it does not restore this test-side Git-list alias invariant. **Candidate:** add the realpath-with-lexical-fallback helper and an owned alias test; retain the existing production worktree lifecycle assertions. |

## Category-4 handoff

Three candidates are deliberately not implemented by this audit fragment:

1. Isolate and clean the pool-restart E2E fixture.
2. Replace purge/preview/pool/diagnostics polling with deferred causal barriers.
3. Restore real-Git worktree alias coverage with a lexical fallback for stale paths.

No source behavior or assertion was modified in this branch.
