# Cross-OS residual reconciliation — group 2

**Audit base:** `origin/main` at `0299fe6b8268f01f136d2a6787983e662e0fdc94` (also this audit branch's starting HEAD).  
**Reference inspected:** `git diff e3051de63cf611143a989f7928bd9f9a7ed9beae^2 e3051de63cf611143a989f7928bd9f9a7ed9beae`.

## Classification legend

1. Merged by a focused PR.
2. Superseded by newer upstream behavior.
3. Intentionally obsolete or inapplicable.
4. Genuinely missing from the audit base.

## Ledger

| Reference file and residual behavior | Classification | Evidence at audit base |
|---|---:|---|
| `src/server/agent/project-registry.ts` — establish dialect-aware canonical project identity: select POSIX versus drive/UNC Windows API from input spelling; retain roots, normalize separators, resolve the longest existing prefix, and preserve a nonexistent suffix unless its parent proves an alias. | 1 | Focused PR **#1077**, `aaae5330` (`Canonicalize cross-platform path identity`), contains this file and is an ancestor of the audit base. Current `canonicalProjectPath()` and `projectPathApi()` implement this contract (lines 139–475). `project-registry-root-paths.test.ts` covers POSIX/drive/UNC, APFS aliases, sensitive volumes, and aliases for nonexistent paths. |
| `src/server/agent/project-registry.ts` — determine case behavior from bounded evidence: alternate-case lookup plus stable directory identity, an eight-entry read-only sample, owned probe cleanup, and conservative fallback; cache only stable non-mutating evidence. | 1 | Current `CASE_EVIDENCE_ENTRY_LIMIT`, `classifyKnownEntry()`, `readOnlyCaseSemantics()`, `probeCaseSemantics()`, and `hasCaseInsensitiveEntries()` (lines 191–431) retain the reference contract. Current root-path tests pin bounded reads, probe cleanup/no probe cache, read-only evidence, cache invalidation, and replacement races. |
| `src/server/agent/project-registry.ts` — use one canonical identity for root availability, `getByPath`, `findByCwd`, Headquarters recognition, and provisional dedupe; use component-aware relative containment and reject mixed path dialects. | 1 | `ProjectRegistry.pathIdentity`, `sameProjectPath()`, `findByCwd()` (lines 513–522 and 856–878), and `registerProvisional()` (lines 1260–1291) are present. The `findByCwd` POSIX/drive/UNC and provisional/HQ alias regressions remain in the two registry test files. |
| `src/server/agent/project-registry.ts` — reference's unconditional lowercasing of foreign Windows descendants, use of `path.win32.relative()` after canonicalization, and treating any same-inode pair as a case alias. | 2 | #1077 deliberately strengthens this: foreign Windows identity normalizes only the drive/UNC root, `findByCwd()` uses slash-normalized `path.posix.relative()` after evidence-based folding, and `classifyKnownEntry()` requires matching directory identities. Current tests `preserves foreign Windows descendant case`, `respects modeled case-sensitive Windows identity components`, and `never treats same-inode file pairs as directory-wide case evidence` pin the safer contract. |
| `src/server/agent/project-registry.ts` — reference's cache publication based only on an optional fingerprint. | 2 | Current `CaseSemanticsCacheRecord` also stores `directoryIdentity`; `hasCaseInsensitiveEntries()` requires stable nonzero fingerprint **and** `dev:ino` before publication. Current tests reject unfingerprinted, zero, and replaced-directory evidence. This is a strict conservative strengthening, not a missing cache invariant. |
| `src/server/agent/resolve-project.ts` — resolve the longest existing prefix for a requested cwd, retaining a nonexistent suffix while recognizing canonical host aliases. | 1 | Focused #1077 commit `aaae5330` contains the file; it is unchanged between that commit and the audit base. Current `realOrResolved()` implements the upward walk (lines 76–92). |
| `src/server/agent/resolve-project.ts` — replace string-prefix cwd ownership with `path.relative()` containment, rejecting sibling-prefix and absolute-relative escapes. | 1 | Current `isSameOrDescendant()` (lines 99–103) retains the reference predicate. `tests2/core/project-preflight.test.ts` and the focused component-path traversal regression cover path-boundary behavior. |
| `tests2/core/project-preflight.test.ts` — pin that case-only spellings of a gateway project emit the gateway-owned warning without relying on the host filesystem's case semantics. | 1 | The exact `case-folded project identities collide without relying on host filesystem case rules` regression is present at lines 175–187, unchanged since #1077. |
| `tests2/core/project-registry-provisional-dedupe.test.ts` — pin canonicalization of missing suffixes only after proof, native sensitive/insensitive Windows behavior, sensitive descendants, and provisional/Headquarters alias dedupe or separation. | 1 | All reference regressions remain in the current test file: `canonicalProjectPath folds nonexistent suffixes`, native Windows sensitive/insensitive tests, sensitive NTFS child test, and the provisional/HQ insensitive and sensitive identity tests. The file is mapped to the v2 core lane in `tests2/tests-map.json`. |
| `tests2/core/project-registry-root-paths.test.ts` — add the cross-host registry matrix: POSIX/drive/UNC roots, APFS aliases, sensitive paths, probe/read-only/cache safety, bounded work, and double-slash duplicate/HQ defenses. | 1 | The file exists and remains registered in `tests2/tests-map.json` as `v2-core`. Its current test inventory retains every reference scenario, including `findByCwd matches POSIX, drive, and UNC`, APFS, sensitive volume, owned/read-only probe, bounded read, cache invalidation, and double-slash tests. |
| `tests2/core/project-registry-root-paths.test.ts` — reference's assertion that all foreign Windows path components case-fold. | 2 | Current #1077 regression intentionally replaces that unsafe assumption with `preserves foreign Windows descendant case while normalizing roots` and modeled sensitive Windows component coverage. This preserves valid per-directory case-sensitive NTFS paths while retaining root normalization. |
| `tests2/integration/project-ui-api.test.ts` — make API project-listing expectations compare the registered canonical fixture root rather than the lexical temporary-directory spelling. | 1 | `fs.realpathSync(rootPath)` is used by the API-created-project regression (lines 49–52); unchanged from #1077. The test is mapped to the v2 integration lane. |
| `tests2/integration/base-ref-api.test.ts` — canonicalize temporary repository fixture roots before registration and construct the warning fixture before its canonical root is captured. | 1 | Current `canonicalPath()`, `fixtureRepo()`, and `beforeAll` fixture setup retain the reference behavior (lines 28–31, 91–98, and 134–160). The test is mapped to the v2 integration lane. |

## Result

All assigned residual reliability contracts are present in focused PR #1077. The two category-2 entries are intentional conservative refinements within that focused path-identity work: they prevent false aliasing on case-sensitive Windows descendants, hard-linked files, and unstable directory observations. No category-3 or category-4 behavior was found; no code or test change is warranted.
