# TEAM-LEAD RESUME STATE (read this first after any compaction/restart)

## ✅ GREEN e2e:v2 + FINAL HONEST RE-MEASURE (merged 96d030a6) — the user's pre-switchover prerequisite is MET
- **e2e:v2 GREEN** under resource caps (node --test-concurrency=1, PW workers=2, sequential, retries:0): 183 pass / 0 fail (~212–229s), Docker-inclusive. 2 broken specs fixed (stories-navigation shims; tail-chat cross-spec cwd-poison → afterEach un-poison). No weakening/skip/relocation. Resource-safe (node_modules flat; caps held; no recrash).
- **EXTERNAL-SERVICE-FREE PROVEN** (fetch-egress-guard.mjs, whole-process-tree): 0 egress; positive control caught github/npmjs, ignored loopback; git-fence rejects non-local. head-to-head §9.
- **FINAL HONEST TOTAL (§10, subtree-CPU authoritative, under peer load, Docker-inclusive both sides): legacy 73.1 vs v2 37.4 CPU-min → ~1.96× CPU** (wall 14.5m vs 10.3m = 1.41× load-caveated; clean-wall ref 16.9 vs 7.5 = ~2.25×). e2e:v2 CPU 5.75 floor → 7.42 complete. **v2 ~2× cheaper, fully green at retries:0, coverage-proven, external-free.** NOT 10× (corrected earlier).
- **FOLLOW-UPS (minor, for switchover/review):** (a) legacy has the SAME pr-walkthrough cwd-poison flake, masked by retries:3 — port the afterEach fix to tests/e2e/ui/pr-walkthrough-pack.spec.ts; (b) legacy unit 1 known env-artifact fail (tool-docs, not a regression); (c) run-v2 exit-1 under load was ONLY the 300s wall-budget cap, not a test failure.
- **NEXT:** the pre-switchover measure the user required is DONE. Remaining: confirm concurrency bar (N=1 + ledger no-thrash) → signal concurrency-proof → switchover (config flip unit→v2 / e2e→e2e:v2 + docs, drop daily-lane gate) → review-findings (tool-manager src-boot fix, concurrency ceiling, the sleep/pr-walkthrough legacy fixes) → ready-to-merge.

## ⚠️ INCIDENT (2026-07-08) — RECOVERED; kill-mechanism NOT confirmed
- **STATUS: env RECOVERED.** Primary node_modules restored (.bin ~111, healthy for master — tsx/vitest absent there is EXPECTED, they're goal-branch-only). Gateway healthy (gate_list 8/12). GOAL worktree node_modules still gutted (.bin=0) — mine to restore via `npm ci` (non-blocking for orchestration; agents npm-ci their own worktrees).
- **CORRECTION to earlier claim:** I asserted the e2e:v2 worktree-teardown deleted THROUGH a node_modules junction. On inspection the runner (run-e2e-v2.mjs) + relocated specs show NO node_modules junction logic, and the project's worktrees use real `npm ci` (worktree_setup_command), not junctions. So the junction-delete-through theory is UNSUPPORTED by code. **More likely:** the heavy e2e:v2 run (20+ node + 7 chrome procs) exhausted resources / crashed the server mid-op, INTERRUPTING an npm ci → the 'packages present, .bin empty' corrupted state. Exact root cause UNCERTAIN.
- **d5bb e2e:v2 WIP PRESERVED** at branch goal/6c956ecf/test-engineer-d5bb @ **d12a3d81** (runner + nonGitCwd-heal + no-new-sleeps + spec-framework/story-registry). Task 7862db76 = BLOCKED.
- **REMEDIATED:** (1) goal worktree node_modules restored (npm ci, 676 pkgs, vitest bin back). (2) run-e2e-v2.mjs RESOURCE-CAPPED on d5bb @ c7692137: Group A node:test serialised (`--test-concurrency=1`, the npm-ci-swarm source) + Playwright workers capped to 2 (env overrides E2E_V2_NODE_CONCURRENCY / E2E_V2_PW_WORKERS). The likely crash cause (Group A running ~CPU-count worktree specs concurrently → npm-ci swarm → exhaustion) is now bounded. Groups already run sequentially + external-free env already present.
- **RESUME PLAN (green-e2e:v2, task 7862db76 blocked):** on a stable box, fresh agent picks up d5bb @ c7692137 → run the CAPPED e2e:v2 (start with defaults; monitor resources) → fix any remaining spec failures → external-calls verify → fair re-measure. Watch for recrash; the caps should prevent it.
- (superseded) DO NOT re-run heavy e2e:v2 until env stable — now capped + node_modules restored.
- (original incident text below, kept for history) 🔴 e2e:v2 KILLED node_modules → SERVER DIED
- **Root cause:** running the **e2e:v2 real-fidelity tier** (worktree/pool specs: continue-archived-worktree*, *-pool, pool-flow, etc.) tore down a worktree **through its node_modules JUNCTION**, deleting the shared node_modules → gateway (runs from primary worktree) lost its deps → server died. SAME hazard class as the chaos.mjs bug (fixed via `unlinkNodeModulesJunction`), but the **e2e:v2 / legacy-e2e worktree teardown was never made junction-safe**.
- **Damage:** node_modules `.bin` wiped across goal (no vitest/playwright) + primary (no tsx). Gateway degraded/port-fluxing (3001/3002) post-restart.
- **DO NOT re-run e2e:v2 (test:e2e:v2 / the worktree-pool specs) until the teardown is JUNCTION-SAFE** — it will kill the server again. This is now a HARD prerequisite before any e2e:v2 run.
- **Recovery needed:** (1) restore node_modules — `npm ci` (primary = operator/infra, I must not touch it + shouldn't npm-ci a running gateway; goal worktree I can); (2) CODE FIX: apply junction-safe teardown (unlink reparse point before recursive delete, + fail-loud guard) to the e2e:v2 worktree path / the worktree-pool spec teardown, mirroring the chaos.mjs fix; (3) THEN re-run e2e:v2.
- Agent test-engineer-d5bb's in-progress e2e:v2 work is PRESERVED uncommitted in its worktree (run-e2e-v2.mjs, e2e-setup.ts nonGitCwd-heal, no-new-sleeps guard, spec-framework.ts/story-registry.ts). Do NOT lose it.
- goal HEAD intact at 873c1dd6; no goal-branch corruption — this is an ENV/node_modules incident, not a code-history loss.



Goal: **Sub-3-min test suite rebuild** (id `6c956ecf-...`). Branch `goal/sub-3-min-test-6c956ecf`.
PR **#935** (base master). This doc is the lead's source of truth; the preview dashboard is only a user summary.
Update this doc whenever state materially changes.

## CLEAN N=2 CHECK (settled) + CUTOVER IN VERIFY
- **Clean all-fixes-in N=2 ×3 = 3/6 green** (merged 9f1b3ade). Up from 0/6 (fixes helped) but NOT clean: rotating integration 60s TIMEOUTS under 2-way CPU starvation (archived-query-search-api, project-isolation, market-pack-team-roles) + 1 browser render-timeout + 1 load-induced 403. Ledger correct (Σ=14/24, no oversubscription). **CONFIRMED: structural server-throughput ceiling at N≥2 on one 24-core box; no test-side lever moves it. Honest bar = N=1.** Validates Josh's concurrency-proof bypass. Checked straight (user's skepticism answered: fixes helped, but ceiling is real).
- **CUTOVER (coder-faa1, branch coder-faa1) committed:** 84ce00cf transparent command routing (unit→test:v2, e2e→test:e2e:v2); 8edfdd80 deleted 880 migrated legacy files (tests-map-driven; SPOT-CHECK VERIFIED: relocate specs + manual-integration(13) + runner + tests2(964) all KEPT; 581 legacy remain); 1b0f5300 docs rewritten to v2 model. VERIFY GREEN (faa1 @ b202ead4): test:unit(v2) tier-1 7147/0 + tier-2 694 pass/1 isolated-load-flake; test:e2e(v2) GREEN Docker-inclusive 213.5s retries:0; guard-v2 --scope core PASS; npm run check 87 errors ALL PRE-EXISTING (A/B proven, 0 new from deletion); 0 dangling import refs; canary safe throughout.
- **🔴 BLOCKING FLAG before merge/switchover — potential COVERAGE LOSS:** 51 migrate-intended stragglers + 153 legacy-pending BROWSER specs have NO v2Path → kept in-repo but NOTHING RUNS them post-cutover. Must characterize: of these 204, how many are behaviourally covered elsewhere in v2 (safe = mapping debt) vs GENUINE coverage loss (assertions run nowhere). faa1 running parity --scope all + sampling to split (a) vs (b). If (b)>0: port before switchover OR user knowingly accepts specific losses. This is the crux of the no-coverage-drop promise — do NOT merge/signal switchover until resolved.
- Other faa1 flags (minor, for review/follow-up): full test:v2 exits 1 under heavy load only on the 300s wall-budget cap (not a test fail — raise the under-load cap per D7); test:v2:browser lacks --project filter → double-runs browser-v2-daily (also in e2e:v2), inflating tier-2 wall (script quirk, follow-up).
- After coverage flag resolved: LEAD merges cutover → signals switchover → review-findings → human confirm for team_complete.

## Where we are (gate DAG)
Passed: baseline/design/di/foundation/mass-migration/browser-tier/parity/**chaos-proof**. **concurrency-proof BYPASSED (Josh, spec change); daily-lane BYPASSED (Josh, not doing daily).** Remaining: **switchover** (unblocked, IN PROGRESS) → **review-findings**. Goal still needs explicit human confirm before team_complete.
- SWITCHOVER (default mapping, user can redirect): flip .bobbit/config/project.yaml unit→`test:v2`, e2e→`test:e2e:v2`, KEEP legacy files (safety net). Rewrite docs (testing-strategy/coverage + AGENTS.md: v2 model, retries:0, honest ~1.96× CPU, N=1+ledger-no-thrash, external-free, NO daily lane) + remove superseded 4-concurrent/retries-3/daily policy w/ pointer. Port pr-walkthrough afterEach cwd-poison fix to tests/e2e/ui/pr-walkthrough-pack.spec.ts. Then signal switchover.
Only the LEAD signals gates. Signal from goal HEAD after merging member branches (verification runs from goal.cwd HEAD).

## Goal HEAD / PR
- Goal HEAD as of writing: **b51be76a** (command-step seam + concurrency harness merged). PR mergeable, 0 behind master (re-check).

## MEASURED HEAD-TO-HEAD (task 190c7af5, merged f0459136) — CORRECTS THE ~10× CLAIM
- Clean quiet-box, subtree-scoped CPU, sequential: **legacy TOTAL 16.9min / 70.6 CPU-min vs v2 TOTAL 7.5min / 30.5 CPU-min → ~2.3× on BOTH wall and CPU.** NOT ~10× — the 10× was inflated (busy-box whole-machine legacy sampling ~130 CPU-min + build vs optimistic v2 target 13). Honest number = **~2.3×**. v2 tier-1+2 is ~24.7 CPU-min, not 13.
- Per-phase apples-to-oranges (v2 folds API-integration into unit → v2 unit slower/does-more); only TOTAL fair. Coverage: v2 unit 7762 tests, strictly more; parity/chaos-proven.
- **CAVEATS (corrected):** Docker exclusion is SYMMETRIC/FAIR — legacy e2e ALSO skips Docker when unavailable (tests/e2e/test-utils/docker.ts `isDockerAvailable()`; Docker-heavy tests already segregated to `-docker`/manual specs). So on this Docker-less box BOTH suites skipped Docker — my earlier 'under-measured favorably to v2 via Docker' was WRONG. Genuine v2-favorable under-count = only the 2 broken v2 specs (stories-navigation broken import, tail-chat-real-stream bash.exe ENOENT) which legacy ran. Other: NO daily/e2e runner exists yet (hand-assembled); external-service-free verification not yet explicitly written; legacy e2e had 8 flaky masked by retries:3. Re-measure task 7862db76 in flight: real e2e:v2 runner + fix the 2 specs + mirror legacy's symmetric Docker-skip + external-calls verify + fair re-measured TOTAL.
- **OPEN DECISION (posted):** given honest ~2.3× (not 10×), proceed to switchover / finish+regreen e2e:v2 first then re-measure / reconsider-scope / my-call. Switchover work remaining: e2e:v2 runner + Docker + fix 2 specs + external-calls verification + sleep/legacy-e2e-preflight-guard cleanup, then config flip + docs.

## DEFINITIVE CONCURRENCY CONCLUSION (all fixes merged f12d3ccf) — N=1 CEILING
- Exhausted the tractable fixes: gateway-boot lease + browser-render lease + ESM-bridge bug + ledger-split bug (activeParents===1) + timing-determinism on named cluster + tool-docs regression. All correct, all merged (f12d3ccf).
- **N=1 ~0-flake (~300s, ~13 CPU-min, 10× cheaper). N≥2 still 0/6** — but now failures are 1–2 ROTATING integration tests/run hitting 60s timeouts under CPU starvation (cast rotates across HUNDREDS of tests: team-lead-child-authz, project-isolation, verification-core, mcp-meta-call, … + intermittent gateway-boot cascade). **STRUCTURAL/HARDWARE CEILING** (2 full test:v2 runs CPU-starve the integration tier on one 24-core box), NOT a test-quality gap — de-flaking is whack-a-mole. ledger Σ≤24 holds (no catastrophic oversubscription).
- **RECOMMENDATION (posted, awaiting user): SHIP ON N=1.** Reframe concurrency-proof to 'single-run clean + ledger caps oversubscription (best-effort under load)', signal it, → switchover. Move structural higher-N (shard browser tier / cut concurrent integration footprint) to the already-proposed 'Restore 5-way concurrency' spin-off. Requires user to bless reframing the gate bar from 5-way → N=1 (a change to original acceptance). Alternatives: structural-change-in-goal (big, delays) / pause-for-review.
- After user blesses N=1 bar: regenerate concurrency-proof.md to PASS-at-N=1 framing, signal concurrency-proof → switchover (flip config to v2 unit/e2e per the reframe decision + docs rewrite) → daily-lane → review-findings (flag the tool-manager src-boot fix + this concurrency ceiling).

## (SUPERSEDED by conclusion above) AUTHORITATIVE CONCURRENCY RESULT (quiet box, merged 32adf29d) + master synced (f8ca70d7, 0 behind)
- **Browser-render lease WORKS & SOLVED the browser tier** (playwright 3/3 at N=3, Chromium pinned cap 4/4). Also fixed a real bug: the .mjs lease client silently fail-opened under Playwright (→ ESM bridge tests/e2e/ledger-lease-bridge.mjs + interop test) — had left BOTH leases dead in the browser tier before.
- **N-curve (retries:0, quiet):** N=1 near-clean; N=2 0/4 (WORSE — ledger split bug: grant≥12 gives full 8v split → 16 vitest workers → oversubscription); N=3 playwright 3/3 but vitest 0/3; wall ~740–786s@N=3. **Binding constraint now = TIER-1 VITEST** rotating timing cluster (verification-command-restart-lifecycle, project-reorder-api, multi-repo-goal[real-git], verification-dedup, proposal-panel-streaming follow-tail, app-smoke keyboard-nav, goal-team-gates archived-child). ~0-flake ceiling on this build = N=1.
- **TWO fixes to reach N≥2 (PENDING USER DECISION posted):** (1) CHEAP: ledger splitBundle full-vitest split only when activeParents===1 (fixes N=2-worse-than-N=3 oversubscription). (2) de-flake the ~7 timing tests via clock seam / observable-state (NO relocation). Recommended = one bounded cycle doing both + re-measure; alt = cheap-first; alt = ship N=1 + defer to spin-off.
- **BONUS production fix landed (f8ca70d7, flag for review-findings):** tool-manager.ts defaultBuiltinToolsDir() added repo-root fallback — under src-boot the dist path didn't exist → ToolManager empty → every role-carrying spawn/delegate failed ROLE_TOOLS_UNRESOLVED. Dist unchanged; 8 test areas re-verified. Real latent bug caught by v2 src-boot.
- Efficiency verdict stands: v2 ~10× CPU, ~3–4× wall, daily-amortised favorable (efficiency-comparison.md merged).

## (DONE) MASTER SYNC #948/#949
- Master +2: **#948 Sidebar Reveal On Nav** (src/app/sidebar-reveal.ts new, main.ts, sidebar.ts; test tests/e2e/ui/sidebar-reveal.spec.ts) + **#949 Market-Pack Roles in Teams** (src/server/agent/{resolve-role,session-manager,session-setup,team-manager,orchestration-core}.ts, server.ts; test tests/e2e/market-pack-team-roles.spec.ts). LIKELY CONFLICTS in DI-clock-seam files session-manager.ts/team-manager.ts/server.ts — use established both-or-neither resolution (keep DI seams + master behaviour). REPRESENT 2 new tests in v2 (sidebar-reveal → browser; market-pack-team-roles → integration). Do AFTER: measurement completes → merge budget+browser-lease result → then this sync.

## CONCURRENCY (Option 1) STATUS
- Command-step DI seam IMPLEMENTED + merged (b51be76a). Validation: contract 13/13, fake specs 35/35 (1 boot, **0 spawns**), real path 10/10, coexistence-isolated, guard-v2 + check green.
- **code-reviewer-8ba9** (session 6a383bec) auditing prod-safety (fake-unreachable-in-prod / verdict integrity / contract strength / fake fidelity) — runs parallel to clusters.
- Prod-safety review PASSED (code-reviewer-8ba9). Hardening merged: fail-closed fake (throws on unrecognised cmd) + stderr contract case.
- **RE-MEASURED (merged 2eda9402):** SEAM WORKED — 3-way wall fixed (was 0/9 @ ~697s → now 524–562s all <600s, Σ≤24); the 5 targeted command-step specs = 0 spawns, 0 flakes across 9 runs. BUT: single 299.7s (<300s ✓); 3×3 = only **2/9 green**; 4-way 0/4 (blows wall 748–770s). **NEW BOTTLENECK = tier-2 BROWSER tier Chromium contention** (browser wall 647–713s) + ~3 real-timer/real-git tier-1 flakes (extension-host-session-event-bus, steer-reconnect, multi-repo-goal). Command-step dimension SOLVED; browser-tier is a separate follow-up. NOTE: comprehensive porting is GROWING the browser tier (571 tests) → worsens contention → tune browser-tier AFTER porting finishes (final size).
- **CONCURRENCY APPROACH PIVOTED (user, settled):** de-flake measurement showed the 3-way flakes are a broad rotating cast of timing-sensitive integration tests starved by transient GATEWAY-BOOT CPU bursts when 3 full suites run concurrently — no static worker-split fixes it. **DECISION: implement a GLOBAL CONCURRENCY BUDGET** — a cross-process lease/token pool (extend the ledger's fs registration) gating the real contention sources (concurrent gateway boots; total Chromium) so heavy ops ACQUIRE→WAIT→RELEASE and the box is never oversubscribed at ANY N. **Accept higher wall-time (queueing) for near-zero flakes** (user: reliability > speed). Flakiness becomes structurally impossible at any N → bar = 'acceptable wall-time at N', NOT a flake threshold — may revive 5-way / obviate the spin-off. **HARD USER DIRECTIVE: do NOT move ANY coverage to daily** (daily = latent bugs). Fix flakes by determinism (clock seam / state waits) + the global budget; NEVER by relocation. Revert multi-repo-goal→daily. Determinism fixes already made (event-bus isolation, steer-reconnect durable assert) kept.
- Re-measure 3/4/5-way ×3 after the budget: green rate (~0 flakes target every N) + per-run wall (rises, accepted) + peak resource ≤ budget. User picks bar on wall-time. Prior agent (1b237f0c) dismissed (context-heavy, split-tuning dead-ends); determinism keepers cherry-picked to goal (824e8e4d). FRESH agent test-engineer-9069 (session 15565bf7) implementing per docs/testing-v2/concurrency-budget-handoff.md.
- **REFRAME UNDER CONSIDERATION (user, 'considering'):** drop the daily-DEFERRAL model. Goal workflows keep `unit:`+`e2e:` where **unit = v2 fast suite**, **e2e = real-fidelity tier (current daily-bucket MINUS manual-integration: Docker/worktree/real-git/spawned-process/MCP)** — BOTH run PER-GOAL under the SAME global budget (queue for resource, don't defer). ONLY manual-integration (real LLM/agents) stays daily. Motivation: no latent-bug window. VIABILITY CRUX (being measured): can the e2e tier hold **0-flake at retries:0 per-goal under the budget**? The budget fixes CPU-contention flakes but NOT environmental ones (Docker daemon, port/TIME_WAIT, container teardown); budget must also gate Docker containers + process spawns + ports (more surface than gateway-boots+Chromium). Fresh agent's efficiency-comparison.md now answers: (a) is v2 unit > legacy, AND (b) is unit+e2e-per-goal viable at retries:0 or does e2e flakiness force some specs to stay daily. DECISION deferred to that data. If viable → rewrites the tier-3 design + switchover config (`unit:`/`e2e:` both v2/real-fidelity per-goal; daily = manual-integration only).
- (SUPERSEDED) prior bar note said signal at 3-way; now bar-by-wall-time under the global budget.
- **BUDGET IMPLEMENTED + BLOCKED ON QUIET BOX (test-engineer-9069, branch test-engineer-9069, HEAD 2a5fc598, task 47507884 BLOCKED — not merged yet).** Built: cross-process gateway-boot lease pool in ledger (acquire→wait→release, fail-open, dead-PID sweep, cap=4; selftest+stress PASS), wired into tier-1 gateway.ts (before the ~11s src import → serialises cold-transform spike) + tier-2 gateway-harness.ts (fail-open, LEGACY UNAFFECTED); Chromium cap 4→2; restored vitest headroom. npm check PASS.
- **KEY FINDING:** gating boots is only HALF the fix. Residual N≥3 flakes are tier-2 `toBeVisible` RENDER timeouts — browser tier needs sustained CPU HEADROOM; even pre-lease headroom hit only 2/9 at 3-way. Boot-lease necessary, not sufficient. **NEXT LEVER NOT YET TRIED: a GLOBAL browser-render lease (the `browser` category cap) — total Chromium across ALL runs capped so runs queue browser work — analogous to the gateway lease.**
- **EFFICIENCY VERDICT (efficiency-comparison.md, answers user's 'is v2 worth it incl daily?'): YES on cost/speed.** CPU-min ~10× (v2 ~13 vs legacy ~130); single-loop wall ~3–4× (5min vs 15–20min); daily amortised favors v2 at any commit rate (K=1→2.2×, K=5→5.9×). The ONLY unproven axis = concurrency reliability (~0 flakes at N≥3) — real risk to weigh at switchover.
- **BLOCKER:** authoritative 3/4/5-way ×3 needs a QUIET box; the dev box has ~46 peer agent procs contaminating runs (rep3 blew 555→660s). Lead can't pause other goals' agents → USER decision: provide a quiet window OR accept measure-under-load as the realistic bar. Agent parked (blocked); re-engage (warm) for the browser-lease + authoritative run once unblocked.
- **SEQUENCING:** run reproducibility cleanup (npm ci rollup lockfile, declare tsx, drop .npmrc shrinkwrap=false) AFTER the de-flake measurement (its npm ci/build would perturb N-way numbers on the single box); config-flip + docs-rewrite wait for concurrency-proof to pass.
- **DAILY LANE (for switchover/daily-lane gate):** 58 daily-bucket entries (45 relocate + 13 adapter) + ~10 manual-integration + 14 v2 daily specs + 1 full legacy run (until retirement). Est ~25–45 min wall (→~15–25 post-legacy-retirement); concurrency LOW by design (1–2; real Docker/LLM/remote/ports don't parallelize). Hard number recorded at daily-lane gate. User idea considered: fold daily into every goal workflow — VERDICT: don't blanket (re-imports the Docker/LLM/port cost the DI work eliminated); the faker-able parts ALREADY moved to per-goal tiers; offer an OPTIONAL targeted real-smoke subset (real-local-git already per-goal via fenced CommandRunner; a Docker/worktree smoke opt-in for goals touching those) while keeping the comprehensive real sweep daily.
- Order: clusters finish denominators FIRST (final browser size) → browser-tier fix + re-measure (needs another quiet window) → signal concurrency-proof.
- **BROWSER PORTING (D8) COMPLETE** (merged 1d5e3eb5): canonical 86-mutant corpus, authoritative --all 0 real holes, v2 83≥legacy 81, 2 v2-stronger, null PASS, only BR26 both-missed (justified), M==N==85, 0 TODO. All porting/consolidation agents dismissed.
- **BROWSER-TIER DE-FLAKE IN FLIGHT** — test-engineer-8605 (session 1b237f0c), task 47507884: reduce tier-2 Chromium contention (tune ledger playwright split / shard) + de-flake ~3 real-timer tier-1 tests (extension-host-session-event-bus, steer-reconnect, multi-repo-goal) via observable-state or relocate-to-daily; re-measure single/3×3/4-way. COMMON prereq for any bar. Started during the user's bar-decision wait (doesn't foreclose the choice). FINAL bar (what N to signal) = user's call.
- STANDING USER DIRECTIVES: (1) keep the preview HTML dashboard updated at milestones; (2) keep the PR mergeable + current with master — re-merge origin/master whenever it advances (it moves often; each sync = a coder resolves conflicts + represents new master tests in v2, then I merge+push).

## Settled decisions (full text in docs/testing-v2/design.md §D7/§D8/§D9; plus:)
- **D7** budget/reliability: single isolated `test:v2` < 5 min (achieved: **251s green**); under 5-way load < 10 min + 0 flakes; budgets = honest measured, never padded.
- **Chaos-fix**: Option A (narrow junction-safety fix) — DONE + verified.
- **D8** browser coverage confidence = switchover prerequisite (mutation + audit + coverage-delta).
- **D9** concurrency: capped at 3 — BUT measured 3-way ALSO fails (see below). SUPERSEDED/OPEN.
- **Coverage closure**: triage-by-mutation, port hybrid, KEPT IN THIS GOAL (comprehensive bounded pass).

## OPEN USER DECISIONS
1. **Concurrency bar** — UNRESOLVED. Measured: single 251s green; 5-way ~800–960s FAIL; 3-way ~570–697s 0/9 FAIL. **CORRECTED ROOT CAUSE (gateway-cost study, docs/testing-v2/gateway-cost-feasibility.md, DONE):** NOT gateway boots — the gateway is already a per-fork singleton (895 integration tests → 6 boots; boot ~470ms lean). The real concurrency killer = the **verification-harness command-steps spawn cmd.exe child processes the LEDGER DOES NOT COUNT** → under N-way the true process count oversubscribes 24 cores → the ~6 heaviest verification tests (gate-reset-api, gates-api-heavy, verification-core, maintenance-api, gate-signal-progress, gate-resign-cancel) hit 60s timeouts. Secondary: ~11s/fork vitest transform of the src/server graph (startup spike). esbuild prebundle (~425ms vs ~11s) helps startup only → SPIN-OFF. **DECIDED (user, settled):** Option 1 = introduce a DI seam for the verification-harness COMMAND-STEP executor (durable detached-shell path ~verification-harness.ts:4732+, separate from the git commandRunner). Default = real durable impl UNCHANGED (fail-closed, fake unreachable in prod); tier-1 gateway fixture injects a contract-faithful FAKE (no shell spawn); a fake⇔real contract test pins fidelity; a FEW real-spawn tests keep crash-recovery/cancel-tree-kill/cmd.exe-fallback + daily runs everything real; heavy gate/API tests (gate-reset-api, gates-api-heavy, maintenance-api, gate-signal-progress, gate-resign-cancel) switch to the fake (verification-core + durability tests stay real). **NO ledger spawn-accounting subsystem** (dropped as unneeded complexity — user's call; base ledger fork/worker allocation stays). Re-measure single-run + N-way (3/4/5) after the mock; confine any residual real-spawn tests cheaply, don't build accounting. Impl task 0bc6a23a assigned to test-engineer c8bf9a3b (session; verify with team_list). After it lands: merge → REVIEW the delicate durability seam (fake must never be prod-reachable / fabricate a verdict) → re-measure → signal concurrency-proof.
2. **Spin-off goal "Restore 5-way test:v2 concurrency"** — PROPOSED, awaiting user review.

## Current roster (verify with team_list; sessions die on restart)
- **test-engineer-1443** `c8bf9a3b-ef5b-4926-9fe4-72552ef12b1e` — task **144a0853** — GATEWAY-COST FEASIBILITY STUDY. Merged harness from branch `goal/6c956ecf/test-engineer-9980` (multi-worktree concurrency-proof.mjs + ledger Σ≤24 fix). Deliverable: docs/testing-v2/gateway-cost-feasibility.md (current boot model + measured cost; shared-gateway-per-fork vs lazy-boot prototype; expected concurrency impact; recommendation). NO gate signal.
- **coder-b93a** `b7246fe1-7364-4800-9428-91febd953950` — task **736906a3** — Browser porting CLUSTER A (journeys: app-smoke, misc, sidebar-nav, prompt-interaction, stories-registry). Corpus `tests2/chaos/browser-mutants-clusterA.json`, `browser-chaos.mjs --corpus clusterA`.
- **coder-51bf** `bfb9ce14-671a-4d2e-a628-e06e1f5b6cbb` — task **b6519897** — Browser porting CLUSTER B (journeys: proposals, goal-editing, project-settings, project-onboarding, team-operations, goal-team-gates, session-lifecycle). Corpus `browser-mutants-clusterB.json`, `--corpus clusterB`.
- Dismissed: coder-9092 (context limit, handoff captured), coder-d894 (master-sync done), coder-ce49/general-a479 (earlier).

## Browser-porting progress (the dominant remaining work; D8 prereq)
- Audit (docs/testing-v2/consolidation-assertion-parity.md): 151 consolidated specs, ~143 GAP/PARTIAL. Mutation showed ~50% of flagged behaviours are REAL holes (~70 est. total).
- **CONSOLIDATION DONE (coder-610a).** Both clusters folded into canonical `tests2/chaos/browser-mutants.json` (86 entries: 2 null + 84 content; cluster A → `BR50-null`/`BR51..BR70`, cluster B → `BR71..BR85`; per-cluster corpus files deleted). Single authoritative `browser-chaos.mjs --all` ran green: **84 content mutants, 81 legacy-caught, 83 v2-caught, 0 REAL holes, null-integrity PASS, only both-missed = BR26 (known), v2-stronger ×2 (BR07, BR70), 134.7 min.** Reconciled unified burn-down + completeness table (N=M=85; Cluster B arithmetic double-count fixed → N=41) in [`browser-chaos-report.md`](./browser-chaos-report.md). Per-cluster tallies/reports marked SUPERSEDED. D8 browser gate evidence complete — awaiting user review/signal.
- _(historical) Cluster B closed N=M=24 (18 holes, authoritative 18/18, null PASS); Cluster A closed N=M=44. Both now unified above._
- Closed + verified so far: ~40 (handoff coder-9092 = 27; Cluster A ~4: BR46/48/51/52; Cluster B ~9: BR45/47/49/50/52/53/54/55/56). ~30 est. remaining.
- Method (proven): per audit-flagged behaviour → add mutant to cluster corpus → `browser-chaos.mjs --corpus <cluster>` → if legacy-caught & journey-missed (REAL hole) port the assertion into the owning journey → re-verify caught. v2 ≥ legacy; retries:0; junction-safe.
- **COMPLETENESS BAR (D8, ENFORCED — clusters kept declaring 'done' prematurely):** 'done' = mutate EVERY distinct JOURNEY-TIER audit-flagged behaviour in the cluster's domains (handoff §1 'remaining' lists + consolidation-assertion-parity.md REC entries), recording each as held / hole-closed / both-missed-justified / excluded-tier — NOT just the confirmed holes. A ~100% hole hit-rate is the tell that only obvious GAPs were mutated; PARTIALs must be mutated too, to PROVE they're held (coder-9092's sweep was ~65% holes). Each cluster tally MUST show an explicit denominator: N flagged → M mutated, ending M==N (0 un-mutated). Excluded (covered elsewhere, do NOT port into journeys): team-ops verification-progress (manual tier), session-status-recovery (daily), prompt-interaction steer-during-bash env-flag repros (legacy), BR26 staff-triggers (both-missed, justified). Cluster B remaining-to-mutate at this checkpoint: proposals(dismiss-reload,revision-autoupdate,spec-survives-navigate), goal-editing(archive-always-on,goal-creation,form-tooltips), project-onboarding(multi-repo-subset,symlink), project-settings(settings-restart-button,goal-accept-failure,goal-reattempt,project-assistant), session-lifecycle(session-created-push-sync). Cluster A: full app-smoke/misc/sidebar-nav/prompt-interaction/stories-registry remaining lists.
- Handoff/how-to: docs/testing-v2/browser-porting-handoff.md; tally: browser-chaos-porting-tally.md.
- BR26 both-missed (staff 'Wake prompt (required)' label) = pre-existing legacy gap, JUSTIFIED (not a regression).

## LEAD's own remaining tasks (do NOT delegate the merge/reconcile)
1. ✅ DONE (coder-610a) — folded cluster A + B corpora into canonical `browser-mutants.json`, de-collided BR ids (`BR50-null`/`BR51..BR70` A; `BR71..BR85` B), deleted per-cluster corpus files.
2. ✅ DONE — reconciled canonical corpus + unified burn-down + completeness table (Cluster B arithmetic double-count fixed) in `browser-chaos-report.md`; per-cluster tallies/reports marked SUPERSEDED.
3. ✅ DONE — ran ONE canonical `browser-chaos.mjs --all`: **0 REAL journey holes**, null PASS, only both-missed = BR26 (known), v2 ≥ legacy (83 ≥ 81). D8 browser gate evidence complete; NOT signalled (user reviews/signals).
3b. (Now delegated to the clusters via per-cluster denominators.) Produce/consolidate an EXPLICIT burn-down checklist: all 143 audit-flagged behaviours (107 GAP + 36 PARTIAL from consolidation-assertion-parity.md) each marked mutated→{held | hole-closed | both-missed-justified}, so completeness is auditable at a glance (not inferred from per-cluster tallies). Second completeness axis = per-file V8 coverage-delta (coverage-delta.mjs) must show no per-area regression.
4. Resolve the concurrency bar (post gateway-cost study) + re-validate.
5. Reproducibility cleanup: **DONE + merged (af2fa1e2).** tsx declared; rollup optional-dep mismatch already fixed on-branch (8e74e6e2); clean `npm ci` verified complete (pi-ai/oauth, pi-agent-core, playwright, sdk, vite, rollup-win32, secure ws/protobufjs). CORRECTION: do NOT remove `.npmrc shrinkwrap=false` — it is LOAD-BEARING (==package-lock=false on npm>=10; freezes the lockfile so a stray `npm install` can't regenerate the vulnerable ws@8.20.1/protobufjs@7.5.9 that pi-coding-agent's bundled shrinkwrap pins & overrides can't penetrate). Kept+documented. CORRECTION: `npm run check` red seen earlier = STALE-WORKTREE artifact (team-lead worktree missing @earendil-works/pi-agent-core -> 39 TS2307 src/ui errors identical to master); fresh `npm ci` is clean — NOT a regression/blocker. Minor non-blocking hygiene: `tsc -p tsconfig.tests2.json` has some tests2/browser (ours) + tool/terminal-pack (pre-existing master) errors -> fold into switchover cleanup.
6. **ARCHITECTURE DECISION (user, settled): NO DAILY LANE.** Real-fidelity tests run as a **per-workflow `e2e:v2` phase** (every workflow: unit:v2 phase → e2e:v2 phase), mirroring legacy's per-commit unit+e2e. manual-integration (real LLM) = separate manual/on-demand invocation in BOTH worlds (never per-commit). **HARD CONSTRAINT (user): e2e:v2 makes ZERO external-service calls — no real LLM, no real GitHub remote, no non-loopback HTTP** — real LOCAL ops only (git/worktree/process/Docker/MCP-subprocess) + the in-process MOCK agent bridge, enforced by the fail-closed fetchImpl + fenced CommandRunner (exactly like legacy e2e). Any real-LLM/external spec belongs in the EXCLUDED manual-integration tier, never e2e:v2. Head-to-head agent verifying this empirically (fetch-fence engaged, zero model calls).
- **PROVENANCE (verified from tests-map):** e2e:v2 specs ARE legacy e2e tests (all `tests/e2e/*`, ~30 relocate real-infra + ~13 adapter) — the irreducibly-real SUBSET of legacy e2e. The BULK of legacy e2e (~340 specs) was migrated into the FAST unit:v2 tier with contract-faithful DI fakes (in-process gateway, fake clock, mock bridge, fenced runner). So v2's cost win = (a) most of legacy-e2e's real ops now run as fakes in unit:v2 + (b) no per-file spawn/transform tax. Legacy e2e is the SUPERSET (runs all ~400 real); v2 e2e:v2 is the real residual (~58). Coverage parity (chaos/parity proofs: v2 ≥ legacy) is what legitimises this — NOT fewer behaviours. Head-to-head = TOTAL-vs-TOTAL (legacy unit+e2e vs v2 unit:v2+e2e:v2). CONSEQUENCES: (a) the **daily-lane gate is DROPPED/REFRAMED** — no staff daily trigger, no tier-3 daily run; (b) switchover config = .bobbit/config/project.yaml `unit:` → v2 fast suite, `e2e:` → v2 real-fidelity phase (all ex-daily-bucket specs minus manual-integration); (c) concurrency is HARDER (every workflow now also runs Docker/real-git e2e) but user accepts budgeted-slower. Head-to-head (task 190c7af5, in flight) measures this exact structure vs legacy unit+e2e.
7. After head-to-head + N=1/bar decision: signal switchover (config flip to v2 unit/e2e-real-fidelity; rewrite docs/testing-strategy.md, testing-coverage.md, AGENTS.md; remove superseded 4-concurrent/retries-3 + daily-lane policy) → review-findings (flag tool-manager src-boot fix + concurrency ceiling) → ready-to-merge. NOTE: goal's original workflow has a daily-lane gate — must be skipped/bypassed given this decision.

## INFRA GOTCHAS (learned painfully — do not relearn)
- **Sweeper hard-resets goal.cwd to origin/<goal-branch>**: local merges/commits are WIPED unless PUSHED. Always `git push` immediately after merging to goal. If a merge "disappears", it was reset to origin — re-merge + push.
- **Verification caches command steps by commitSha**: a stale/poisoned cached result (e.g. an old campaign) is reused on re-signal at the same commit. BUST with a fresh commit (even `--allow-empty` won't help if tree identical → make a real/empty NEW-sha commit; a new merge commit works).
- **node_modules corruption**: chaos.mjs & browser-chaos.mjs junction node_modules into ephemeral worktrees; `unlinkNodeModulesJunction` unlinks the reparse point BEFORE any recursive delete. NEVER regress this — delete-through-junction wipes the shared tree (bricked agent spawning once). RCA: docs/testing-v2/node-modules-corruption-rca.md.
- **tsx** is undeclared; legacy/chaos legacy-tier uses `npx --no-install tsx`. **@earendil-works/pi-ai** (with `./oauth` subpath, dist/oauth.js) is needed for v2 tests that import the server graph; goal worktree nm can be incomplete → copy from primary `C:/Users/jsubr/w/bobbit/node_modules/@earendil-works/pi-ai` if missing.
- **API auth**: `BOBBIT_TOKEN` + `BOBBIT_GATEWAY_URL` env work for REST GET. PUT `/api/goals/:id` (edit goal spec) = **403 sandbox token cannot access** → cannot edit goal-spec prose; record decisions in design.md instead. `PUT` (not PATCH) is the goal-update verb.
- **git commit/push via Bash**: a trailer-injector mangles commands containing `(`/parens/certain text → use `git commit -F- <<'EOF' ... EOF` (heredoc) for messages, and run `git push` as its own bare command. Co-author trailer: `Co-authored-by: bobbit-ai <bobbit@bobbit.ai>`.
- **Machine**: single 24-core thermally-throttled box. Serialize heavy runs (5×3 proofs, chaos --all campaigns). Targeted browser-chaos runs are low-worker (~2 gateways) so 2–3 in parallel is fine (~6 boots << the ~15–20 that starves). Coordinate heavy runs through the lead.

## ETA
Switchover-ready ~2–3 working days (browser porting ~57% done + parallel; then concurrency/repro/switchover/daily/review). Legacy retired ~2 weeks after (14 consecutive green daily runs; legacy stays the gate/safety-net until then).
