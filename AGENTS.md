# Bobbit — Agent Guide

## Commands

```bash
npm run build          # Full build (server + UI)
npm run build:server   # Compile server TypeScript only
npm run build:ui       # Vite bundle UI only
npm run dev            # Gateway + vite dev server with hot reload
npm run dev:harness    # Gateway via restart harness + vite (use this for development)
npm run restart-server # Signal the harness to rebuild & restart the server
npm start              # Run built gateway (serves embedded UI)
npm run check          # Type-check both server and web without emitting
npm test               # Run all tests (unit + E2E)
npm run test:unit      # Unit tests — Node test runner + Playwright file:// fixtures (<30s)
npm run test:e2e       # E2E tests — API (in-process) + browser (spawned gateway)
npm run test:manual    # Manual integration tests — real agents + Docker (~5 min)
SCREENSHOTS=1 npm run test:manual  # Same + browser screenshots + HTML report
```

### Dev server harness

Use `npm run dev:harness` for development. The harness watches `.bobbit/state/gateway-restart` — run `npm run restart-server` to rebuild and restart after server changes. Auto-restarts on crashes. Sessions survive restarts via `.bobbit/state/sessions.json`.

## Development workflow

See [docs/dev-workflow.md](docs/dev-workflow.md).

**Quick reference**: UI changes (`src/ui/`, `src/app/`) hot-reload automatically. Server changes (`src/server/`) require `npm run restart-server`. Always `npm run check` before restarting.

## Testing

```bash
npm run check                    # Type-check first
npm run test:unit 2>&1 | tail -5 # Unit tests (<30s, no server)
npm run test:e2e 2>&1 | tail -5  # E2E tests (API in-process + browser spawned)
npm run test:manual              # Manual integration tests — real agents + Docker (~5 min)
```

Test filter flags: `--failures` (default), `--verbose`, `--full`.

**When to run what:**
- UI-only changes → unit tests
- Server changes → unit + E2E
- Session lifecycle, sandbox, worktree, or restart changes → also `npm run test:manual`

Docker-dependent tests skip automatically when Docker is unavailable.

**Test types:**
- **Unit** (`tests/*.spec.ts`, `tests/*.test.ts`): Playwright `file://` fixtures + Node test runner.
- **API E2E** (`tests/e2e/*.spec.ts`): In-process gateway. Import from `./in-process-harness.js`.
- **Browser E2E** (`tests/e2e/ui/*.spec.ts`): Spawned gateway + Playwright. Import from `../gateway-harness.js`.
- **Manual integration** (`tests/manual-integration/`): Real agents, real Docker. Not in CI.

See [docs/testing-strategy.md](docs/testing-strategy.md) for harness selection, helper APIs, mock agents. See [docs/testing-coverage.md](docs/testing-coverage.md) for per-area coverage (prompt interactions, sidebar, sessions & resilience, sidebar child auto-loading).

**Rules:**
- Tests run in isolation — never read/write `.bobbit/` directly, use the isolated directory from `e2e-setup.ts`.
- **Never start background servers from bash** (`node server.js &`) — pipes hang the agent. Use Playwright `webServer` config.
- Prefer `file://` fixtures for new tests. Use E2E only when you need a real server.

**E2E coverage requirement**: Every feature that changes user-facing behavior MUST include a browser E2E test covering (1) navigation, (2) happy path, (3) persistence across reload, (4) cleanup/undo. Canonical pattern: `tests/e2e/ui/settings.spec.ts`. Bug fixes need only a unit test unless the bug involves multi-step server interactions.

## Recipes

One-liner task → entry point. Follow links for walkthroughs.

- **Add an API E2E test** → `tests/e2e/`, import `./in-process-harness.js`. See `gates-api.spec.ts`.
- **Add a UI E2E test** → `tests/e2e/ui/`, import `../gateway-harness.js`, helpers `./ui-helpers.js`. See `session-interactions.spec.ts`.
- **Add a Tier 2.5 video-capturing E2E test** → import `tests/e2e/ui/fixtures.ts` instead of `gateway-harness.ts`, call `await rec.capture(label)` at UX moments, run with `RECORDSCREEN=1`. See [docs/testing-tier-2-5.md](docs/testing-tier-2-5.md).
- **Add a REST endpoint** → `handleApiRoute()` in `src/server/server.ts`. See [docs/rest-api.md](docs/rest-api.md).
- **Add a project** → `POST /api/projects` or sidebar "Add Project". Key: `project-registry.ts`, `project-context.ts`. The project-assistant proposal panel renders three views (Components / Workflows / Diff) on top of a legacy editable-fields block; testids `view-tab-{components|workflows|diff}`, `component-card-${name}`, `workflow-card-${id}`, `gate-node-${gateId}`, `step-badge-${type}`. The panel updates live across multiple `propose_project` calls — see the staleness debugging entry below for the shallow-merge mechanism. To tweak a single field of an in-flight project proposal without re-emitting the full payload, the assistant uses `view_proposal` / `edit_proposal` (see "Edit a proposal mid-session" below). **The project assistant is solely responsible for designing workflows — the server no longer seeds defaults.** A `propose_project` call that omits `workflows` results in a project with zero workflows; the assistant must derive bespoke workflows from the discovered components/commands. Per-component and all-components scaffolds (`buildPerComponentWorkflow` / `buildAllComponentsWorkflow` in `src/server/state-migration/per-component-workflows.ts`, reusing named exports from `seed-default-workflows.ts` including `RALPH_LOOP_DESCRIPTION` and `readyToMergeGate()`) remain available as adaptable starting points the assistant chooses explicitly when they fit — they are not pre-checked by component count. Monorepo subprojects (pnpm/npm/yarn workspaces, Nx, Turbo, Lerna, Cargo, Go, Gradle) are auto-detected by `src/server/agent/monorepo-scan.ts` (capped at 30 candidates) and surfaced into the assistant prompt. The Ralph-loop framing for `implementation` gates lives in `defaults/workflow-authoring-guide.md` §3.1 and is inlined into the assistant prompt at module init via `readFileSync`. See [docs/internals.md — Project assistant](docs/internals.md#project-assistant) and [No default workflow scaffold](docs/internals.md#no-default-workflow-scaffold).
- **Remove a project** → settings → General → Remove. `DELETE /api/projects/:id`. Refuses at zero projects (400; `?force=1` under `BOBBIT_E2E=1`).
- **Add a WebSocket command** → `ClientMessage` in `ws/protocol.ts`, handle in `ws/handler.ts`, add `RpcBridge` method.
- **Add a UI component** → `src/ui/components/`, export from `src/ui/index.ts`. Review pane details in [docs/review-pane-mobile.md](docs/review-pane-mobile.md).
- **Add a tool renderer** → `src/ui/tools/renderers/`, register in `src/ui/tools/index.ts`. See `ProposalRenderer.ts`.
- **Modify slash-skill expansion / chip rendering** → server resolver `src/server/skills/resolve-skill-expansions.ts` (snapshot semantics, prefix-only vs inline scan), sidecar persistence `src/server/skills/skill-sidecar.ts`, system-prompt section in `src/server/agent/system-prompt.ts`, autonomous-activation tool `defaults/tools/skills/activate_skill.yaml` + `defaults/tools/skills/extension.ts`. UI chip `src/ui/components/SkillChip.ts`, splice into user bubble in `src/ui/components/Messages.ts`, tool-call rendering in `src/ui/tools/renderers/ActivateSkillRenderer.ts`. See [docs/design/skill-ux-and-autonomous-activation.md](docs/design/skill-ux-and-autonomous-activation.md).
- **Label multi-line bash calls** → pass `description` to `bash` (mirrors `bash_bg`'s required `name`). See `defaults/tools/shell/bash.yaml`.
- **Add a tool** → builtin: `defaults/tools/<group>/`. Project override: `.bobbit/config/tools/<group>/`. MCP auto-discovered from `.mcp.json`. See [docs/internals.md — MCP servers](docs/internals.md#mcp-servers).
- **Add a blocking tool** → harness parks Promise keyed by `(sessionId, toolUseId)`; later REST call resolves it. See [docs/blocking-tools.md](docs/blocking-tools.md). (`ask_user_choices` is non-blocking — see [docs/non-blocking-ask.md](docs/non-blocking-ask.md).)
- **Add a slash skill** → `SKILL.md` in `.claude/skills/<name>/` with YAML frontmatter. See [docs/internals.md — Config scan directories](docs/internals.md#config-scan-directories).
  - Discovered skills are auto-exposed to the agent (level-1 progressive disclosure): name + description are listed in the system prompt under "Available Skills" and the agent can self-activate via the `activate_skill` tool. Skills with `disable-model-invocation: true` (or roles with `Skills: never` policy) stay user-invocable only. User-typed `/name args` renders as a clickable chip in the chat bubble while the model still receives the fully expanded body. See [docs/internals.md — Skill chip rendering & autonomous activation](docs/internals.md#skill-chip-rendering--autonomous-activation).
  - **Multi-file skills (Level-3 progressive disclosure):** SKILL.md authors can include `references/`, `scripts/`, and `assets/` subdirs. On activation, the agent receives a synthetic header with the skill root absolute path and a one-level-deep resource manifest, then reads referenced files on demand using the relative paths the SKILL.md author wrote. `@path` markdown refs in skill bodies are NOT auto-inlined — Claude Code parity. Built-in and personal skills emit a degraded header inside the Docker sandbox; see [docs/internals.md — Sandbox skill visibility](docs/internals.md#sandbox-skill-visibility).
- **Add an IndexSource** → implement `IndexSource` from `src/server/search/types.ts`; wire into `SearchService`. See [docs/design/portable-search.md](docs/design/portable-search.md).
- **Add a goal feature** → `goal-manager.ts` / `goal-store.ts`, REST in `server.ts`, assistant in `goal-assistant.ts`. See [docs/goals-workflows-tasks.md](docs/goals-workflows-tasks.md).
- **Add a nested-goal field** → extend `PersistedGoal` in `src/server/agent/goal-store.ts`; the lazy-migration in `load()` handles missing fields automatically (compute defaults at use sites, never backfill at the data layer). Update `tests/goal-store-nesting.test.ts` round-trip cases and the field table in [docs/goals-workflows-tasks.md — Nested goals](docs/goals-workflows-tasks.md#nested-goals-phase-1-data-model). For child-spawn flows: `GoalManager.createGoal(title, cwd, { parentGoalId, workflowId, ... })` auto-derives `rootGoalId` (`id` for root, `parent.rootGoalId ?? parent.id` for child) and `mergeTarget` ("master" for root, "parent" for child) and walks the parent chain for cycle prevention (capped at `NESTING_WALK_DEPTH_CAP = 64`). The pure helper `deriveNestingFields(newId, parentGoalId, lookup)` is unit-testable. See [docs/nested-goals.md](docs/nested-goals.md).
- **Add a subgoal verify-step** → set `type: "subgoal"` on a workflow gate's `verify[]` entry with a `subgoal: { planId, title, spec, workflowId?, suggestedRole? }` payload. The harness's `runSubgoalStep` (`src/server/agent/verification-harness.ts`) spawns / resolves a child goal via `resolvePlanStepChild` (tier-based: live in-progress > archived complete > live other > archived non-complete), waits for the child's `ready-to-merge` to pass, then merges + archives. Concurrency: per-`rootGoalId` semaphore (default 3, hard max 8) acquired before spawn, released in finally. The very next call after `createGoal` MUST be `updateGoal({ spawnedFromPlanId })` (Lesson 4.1). See [docs/nested-goals.md — The subgoal verify-step type](docs/nested-goals.md#the-subgoal-verify-step-type) and [docs/goals-workflows-tasks.md — subgoal step type](docs/goals-workflows-tasks.md#subgoal-step-type).
- **Bind an ephemeral role or workflow to one goal (no project-library pollution)** → mirror the existing `goal.workflow` snapshot pattern. Pass `inlineRoles: Record<roleName, Role>` on `POST /api/goals` (or `goal_spawn_child` / `propose_goal`) and the role is deep-cloned onto `PersistedGoal.inlineRoles`. The verification harness + `team-manager.spawnRole` resolve role names via the pure helper `resolveRole(goal, name, roleStore)` in `src/server/agent/resolve-role.ts` — inline first, project/server/builtin role-store second; `listAvailableRoles(goal, roleStore)` lists both for fail-loud error messages. Children spawned via `goal_spawn_child` inherit `parent.inlineRoles` automatically (server-side merge `{...parent, ...body}`; child entries with the same name override the parent's). Same pattern for ephemeral workflows: `body.workflow` on `POST /api/goals` and `goal_spawn_child` snapshots the full Workflow onto `goal.workflow`, bypassing the project workflow store. Tool definitions surface this trade-off explicitly: `propose_role` is for permanent roles; `inlineRoles` on `propose_goal` / `goal_spawn_child` is for one-off cases. Tests: `tests/resolve-role.test.ts`, `tests/goal-manager-inline-roles.test.ts`, `tests/e2e/api-goals-spawn-child-route.spec.ts`.
- **Drive nested-goal lifecycle from the team-lead** → nine team-lead-only tools in group `Children` (`defaults/tools/children/`): `goal_spawn_child`, `goal_plan_propose`, `goal_plan_status`, `goal_merge_child`, `goal_pause`, `goal_resume`, `goal_archive_child`, `goal_decide_mutation`, `goal_set_policy`. Every cascade-affecting REST call (`pause`, `resume`, `archive`) requires explicit `cascade: boolean` — server returns 422 `CASCADE_REQUIRED` when omitted. `POST /api/goals/:id/team/teardown?cascade=true|false` mirrors the same contract for stopping a parent's team — without `cascade=true` the server pre-flights descendant teams and returns **409 `HAS_DESCENDANT_TEAMS {count, descendants:[{id,title}]}`**; with `cascade=true` it walks descendants depth-first (best-effort per-goal, returns `{ok, toreDown, errors[]}`). Client wrapper `teardownTeamWithDialog(goalId)` in `src/app/api.ts` pre-flights `cascade=false` and on 409 opens `showStopTeamDialog` (`src/app/dialogs.ts`) to confirm; the dashboard's `handleEndTeam` routes through it. UI is the cascade-policy authority. Mutation classifier (`src/server/agent/plan-mutation.ts`) reports `noop | fix-up | expansion | restructure | criteria-drop`; matrix lives in [docs/nested-goals.md — Mutation classifier](docs/nested-goals.md#mutation-classifier). Pending mutation requests persisted at `<stateDir>/plan-mutations/<goalId>.json` with 24h TTL. Contributor-role policy: every contributor declares `never` for all nine tools; pinned by `tests/role-children-tools-policy.test.ts`.
- **Inject nested-goals awareness into a team-lead's system prompt** → `buildNestingContextSection({ team, goalBranch, parent?, root? })` in `src/server/agent/system-prompt.ts`. Caller passes a `NestingContext` via `parts.nestingContext`. Stanza A (root, `parent === undefined`): "you raise the PR; mention `maxConcurrentChildren` + `divergencePolicy`". Stanza B (child, `parent !== undefined`): "your branch merges INTO parent's branch LOCALLY. **DO NOT raise a PR.**" Stanza C (decision rule, always for team goals): `task_create` vs `team_spawn` vs `subgoal` table. Non-team goals → returns `undefined`. Backstop: a single-paragraph "Goal nesting awareness" pointer in `defaults/roles/team-lead.yaml::promptTemplate`. Tests: `tests/system-prompt-nesting-stanzas.test.ts`.
- **Render nested goal trees / plan DAG** → pure helpers (no DOM, no Lit, unit-testable): `src/app/sidebar-nesting.ts` (`buildNestedGoalForest`, depth cap 5, descendantCount aggregation, "Show N more child goals…" overflow), `src/app/plan-synthesis.ts` (`buildPlanSteps` — formal plan + ad-hoc orphan append OR createdAt-clustered living plan, Lesson 4.20), `src/app/plan-node-state.ts` (`resolvePlanNodeChild` — tier-based, MUST agree with server's `resolvePlanStepChild`), `src/app/plan-edge-paths.ts::computeEdgePaths` (3-segment right→left connectors that exit the source's right edge and enter the destination's left edge with a vertical jog at `xmid` through the inter-phase X-band — keeps every edge inside the empty band between phase columns so it never crosses a node; `midLineX` opt biases that x; legacy `midLineY` retained only for back-compat tests), `src/app/goal-dashboard-tab-visibility.ts` (`shouldShowPlanTab` / `shouldShowChildrenTab` / `shouldShowTasksTab`). Plan tab DAG renders nested sub-trees inline with chevron disclosure (depth cap 3, Lesson 4.22); subscribe to `goal_state_changed` and `goal_child_spawned` WS events with 250ms trailing-edge throttle.
- **Cascade confirmation dialogs** → three new dialogs in `src/app/dialogs.ts`: `showArchiveGoalDialog` (descendants always cascaded — checkbox read-only because session teardown is intrinsic), `showPauseGoalDialog` and `showResumeGoalDialog` (toggleable cascade checkbox). Wired into `deleteGoal`, dashboard archive/pause/resume buttons. Goals with zero descendants fall through to the existing `confirmAction("Archive Goal", …)` flow — no UX regression for single-goal users. Mutation-approval card uses reducer actions `mutation-pending` and `mutation-update` keyed by `requestId`; flips to a decided-disabled state on `mutation_decided`.
- **Compute tree cost across descendants** → `GET /api/goals/:goalId/tree-cost` walks the descendant tree via the `rootGoalId` chain (BFS, depth cap 32) and sums per-goal cost. Handler resolves the rollup root as `goal.rootGoalId ?? goal.id` so a request against a child still returns the WHOLE-TREE rollup. Pure helper: `src/server/agent/cost-tracker.ts::computeTreeCost(rootGoalId, allGoals, costTracker, sessionIdsForGoal)`. Cache lives on the existing `CostTracker` (per-tracker WeakMap keyed by `rootGoalId`); `costTracker.getGeneration()` ticks monotonically on every cost mutation, so cache invalidation is free. Breakdown sorted `depth ASC, createdAt ASC`; archived descendants still counted; cross-tree goals excluded. Dashboard renders `Tree cost: $X.XX` above per-goal cost; click expands a per-child breakdown. See [docs/nested-goals.md — Cost rollup](docs/nested-goals.md#cost-rollup).
- **Run mutation classification on a frozen plan** → `classifyMutation(prevSteps, proposedSteps, rootSpec, criteria)` in `src/server/agent/plan-mutation.ts`. Pure module imported by `PATCH /api/goals/:id/plan` and unit tests. Severity order: `noop < fix-up < expansion < restructure`; the classifier reports the most severe structural kind, then the criteria-coverage check OVERRIDES to `criteria-drop` if any root acceptance criterion is uncovered. Coverage is **whitespace-normalised, case-insensitive substring** match over `{rootSpec, every proposed step's spec}`. Decision matrix: `fix-up` auto-applies under `balanced`/`autonomous`; `expansion` always queues for approval; `restructure` requires `paused` (else 409 `RESTRUCTURE_REQUIRES_PAUSE`); `criteria-drop` always 409. `replanCount` increments per applied/approved mutation; `> 5` auto-pauses for human review. Tests: `tests/plan-mutation.test.ts`.
- **Inter-agent git handoff** → tasks carry `baseSha`, `headSha`, `branch`. See [docs/goals-workflows-tasks.md — Git handoff](docs/goals-workflows-tasks.md#git-handoff-fields).
- **Modify a store constructor** → stores take `stateDir`/`configDir` params; never module-level globals. All resolution via `ProjectContextManager`. See [docs/internals.md — Store resolution](docs/internals.md#store-resolution-pattern).
- **Add/modify session creation** → plan/execute in `session-setup.ts`, wrappers in `session-manager.ts`. Four modes: normal, worktree, delegate, continue-archived. See [docs/internals.md — Session worktrees](docs/internals.md#session-worktrees).
- **Pool claim flow** → see [docs/design/remove-session-worktree-rename.md](docs/design/remove-session-worktree-rename.md) and `worktree-pool.ts::claim()`. Branch + container directory are produced at claim time as `session/<id8>` and `<wtRoot>/session-<id8>/`. No deferred rename.
- **Continue an archived session** → footer button in `AgentInterface.ts` → `ContinueSessionChooser.ts` (confirm-only — no mode radio, no large-transcript warning) → `POST /api/sessions/:archivedId/continue` (empty body; legacy `mode` field is tolerated but ignored). Lossless: server clones the source `.jsonl` to a fresh path computed by `formatAgentSessionFilePath` (`src/server/agent/agent-session-path.ts`) via `sessionFileCopy` (`src/server/agent/session-fs.ts`), persists the path as `agentSessionFile` on the new session row before spawn, and the agent CLI rehydrates from it via `switch_session` — same mechanism restart-resume uses. No transcript stringification, no `seedContext`, no 128 KB cap. `CrossRealmCopyError` (host↔sandbox or cross-project) → 422. For worktree-backed sources, `executeWorktreeAsync` rebases the cloned `.jsonl` from the project-root slug-dir onto the worktree-cwd slug-dir before issuing `switch_session` — the continue handler pre-computes `destJsonl` against `proj.rootPath` but the agent CLI boots with `cwd = offsetCwd` and `formatAgentSessionFilePath` embeds a `cwd`-derived slug. See [docs/internals.md — Continue-Archived sessions](docs/internals.md#continue-archived-sessions) and [docs/design/lossless-continue-archived.md](docs/design/lossless-continue-archived.md).
- **Archived session WS handshake** → on initial connect the server pushes a `state` frame carrying the persisted `model` + `imageGenerationModel` so the client's hardcoded placeholder model in `src/app/remote-agent.ts` is replaced before the footer renders. Single source of truth for the payload is `buildArchivedStateData()` in `src/server/ws/handler.ts`, used by both the archived `auth_ok` branch and the `get_state` handler. See [docs/internals.md — Archived session WS handshake](docs/internals.md#archived-session-ws-handshake).
- **Server-side read/unread state** → `lastReadAt` on `PersistedSession`; `POST /api/sessions/:id/mark-read`. Client uses `markSessionVisited` / `hasUnseenActivity` in `src/app/render-helpers.ts`. Legacy `localStorage` map migrated once via `migrateLegacyVisitedMap` from `src/app/main.ts`. See [docs/internals.md — Read/unread state](docs/internals.md#readunread-state).
- **Config cascade** → builtin → server → project, for **roles, tools, and tool-group-policies only**. Workflows are NOT cascaded — they live inline per-project in `project.yaml::workflows` and have no system-scope layer (`/api/workflows*` mutations require `projectId`; no `?scope=server`). Modules: `builtin-config.ts`, `config-cascade.ts`. See [docs/internals.md — Config cascade](docs/internals.md#config-cascade) and [Workflows are project-scoped only](docs/internals.md#workflows-are-project-scoped-only).
- **Add a config type to the cascade** → loader in `BuiltinConfigProvider`, `resolveX()` in `ConfigCascade`, REST endpoint, UI scope row (`config-scope.ts`).
- **AI Gateway per-session routing header (`x-opencode-session`)** → `writeAigwModelsJson` in `src/server/agent/aigw-manager.ts` emits a provider-level `headers` block on the `aigw` provider entry of `~/.bobbit/agent/models.json`. The header value is a pi-coding-agent `!cmd` shell-resolver that prints `BOBBIT_SESSION_ID` from the agent subprocess env (every spawn path already injects it). When the env var is empty, `resolveConfigValue` drops the header entirely — never sent with a fallback constant, so on-prem token caches stay partitioned per session. Provider-level only → applies to `openai-completions` models; Bedrock ignores `model.headers`. See [docs/internals.md — AI Gateway per-session header](docs/internals.md#ai-gateway-per-session-header-x-opencode-session).
- **Debug review/naming model picking the wrong model under AI Gateway** → `applyReviewModelOverrides` in `src/server/agent/review-model-override.ts` parses `default.reviewModel`/`default.namingModel` as `provider/modelId`, calls `rpc.setModel`, then `rpc.getState()` and throws on mismatch (accepts both `{success,data:{model}}` and `{model}` shapes). Naming fallback: `title-generator.ts::pickFallbackAigwNamingModel` tiers haiku→sonnet→opus. See [docs/debugging.md](docs/debugging.md).
- **Change tool access policy** → `defaults/tool-group-policies.yaml` (builtin) or `.bobbit/config/tool-group-policies.yaml` (project). Per-role: role YAML `toolPolicies`. Values: `allow`/`ask`/`never`. See [docs/internals.md — Tool access policies](docs/internals.md#tool-access-policies).
  - **`gate_signal` is team-lead-only.** Every contributor role YAML in `defaults/roles/` (`coder`, `test-engineer`, `reviewer`, `code-reviewer`, `security-reviewer`, `architect`, `spec-auditor`, `qa-tester`, `docs-writer`) declares `toolPolicies.gate_signal: never`; only `team-lead.yaml` has `always-allow`. The tool-guard extension hard-blocks the call at runtime so contributor agents that try to signal a gate get an explicit refusal. Invariant enforced by `tests/role-gate-signal-policy.test.ts`; generator coverage in `tests/tool-guard-extension.test.ts` (`gate-signal-never` variant). See [docs/goals-workflows-tasks.md — Signaling a gate](docs/goals-workflows-tasks.md#signaling-a-gate).
- **Per-role model / thinking-level override** → role YAML `model: "<provider>/<modelId>"` and `thinkingLevel`. Edit via the **Model** tab on the role-manager page (`src/app/role-manager-page.ts`). Bound at session start by `tryAutoSelectModel` / `tryApplyDefaultThinkingLevel` in `session-manager.ts` via the new `applyModelString` helper in `review-model-override.ts`. Verification reviewer/QA sessions check the role first in `verification-harness.ts`, then fall back to `default.reviewModel`. Cascades project > server > builtin like `toolPolicies`. Spec: [docs/design/per-role-model-overrides.md](docs/design/per-role-model-overrides.md); summary: [docs/internals.md — Per-role model & thinking-level overrides](docs/internals.md#per-role-model--thinking-level-overrides).
- **Change message rendering** → `src/ui/components/Messages.ts` (standard), `message-renderer-registry.ts` (custom).
- **Modify message transcript ordering / reducer** → all transcript mutations route through the pure `reduce(state, action)` in `src/app/message-reducer.ts` (13 action types, single sort key `(_order, _insertionTick)`). `RemoteAgent` in `src/app/remote-agent.ts` is a thin dispatcher — never push directly into `state.messages`. Server stamps `_order` via `seq` (live, in `src/server/agent/event-buffer.ts::pushFrame`) or `SNAPSHOT_ORDER_FLOOR + i` (snapshot, in `src/server/ws/handler.ts`). Render keys by id (synthetic fallback `synth:${origin}:${order}:${tick}`) — no index keys, no render-time sort. See [docs/internals.md — Reducer ordering invariant](docs/internals.md#reducer-ordering-invariant) and [docs/design/unified-message-ordering-reducer.md](docs/design/unified-message-ordering-reducer.md).
- **Compute the streaming-message id (in-flight assistant `message_end` with tool calls)** → use `computeStreamingMessageId(msg)` from `src/app/streaming-message-id.ts`. Real LLM streams sometimes deliver assistant `message_end` without a string `id` (undefined / null / numeric / `0` / `""`); the helper falls back to `synth:tc:<firstToolCallId>` so the visible-messages filter in `AgentInterface.ts` can still hide the in-flight row by id-equality. Both call sites in `RemoteAgent` (the `streamingMessageId` assignment **and** the reducer entry's `id` stamp) must go through the helper or the two will diverge and the card renders twice. Do not reintroduce inline `typeof msg.id === "string" ? msg.id : undefined`. See [docs/internals.md — Streaming message id (synthetic fallback)](docs/internals.md#streaming-message-id-synthetic-fallback).
- **Modify proposal panel streaming UX** → streaming flag in `state.proposalStreamingByTag` (writer: `RemoteAgent._checkToolProposals` in `src/app/remote-agent.ts`; bulk-clear on `agent_end` / `reset()`); per-panel reads + `streamingBadge()` / `STREAMING_BORDER` / `data-testid="proposal-primary-submit"` in `src/app/render.ts`; scroll/selection preservation across deltas via `reconcileFollowTail` in `src/app/follow-tail.ts` (WeakMap-keyed lock state, 5px tail, programmatic-scroll echo filter, mirrors chat scroll lock invariant). See [docs/internals.md — Proposal panel scroll lock invariant](docs/internals.md#proposal-panel-scroll-lock-invariant) and [docs/internals.md — Proposal streaming flag](docs/internals.md#proposal-streaming-flag).
- **Git-status widget** → `src/ui/components/GitStatusWidget.ts`, `AgentInterface.ts`, `src/app/session-manager.ts`, `src/app/goal-dashboard.ts`, `src/app/git-status-refresh.ts`, `src/app/api.ts`, `src/server/server.ts` (`batchGitStatus`), `src/server/skills/git-status-native.ts` (host-path parallel `execFile`). See [docs/design/git-status-widget-reliability.md](docs/design/git-status-widget-reliability.md) and [docs/internals.md — Git status cache & client resilience](docs/internals.md#git-status-cache--client-resilience).
- **Large content truncation** → `truncate-large-content.ts` (>32KB). Applied in `session-setup.ts`/`session-manager.ts`. Renderers: `WriteRenderer.ts`, `PreviewRenderer.ts`. Lazy-load via `GET /api/sessions/:id/tool-content/:mi/:bi`. See [docs/internals.md — Large content truncation](docs/internals.md#large-content-truncation).
- **Modify sandbox behavior** → `sandbox: "docker"` in `project.yaml`. Key: `project-sandbox.ts`, `sandbox-manager.ts`, `docker-args.ts`. See [docs/internals.md — Docker sandbox](docs/internals.md#docker-sandbox).
- **Add a multi-repo project / components / inline workflows** → components live in `project.yaml::components[]` (the only collection); workflows live inline in `project.yaml::workflows` (project-scoped only — no cascade, no system layer; `/api/workflows*` mutations require `projectId`). **Workflows are NOT auto-seeded** — a project may legitimately have zero workflows, and the project assistant is responsible for designing them from the project's actual components and commands (see [No default workflow scaffold](docs/internals.md#no-default-workflow-scaffold)). Default-component name = project name. Data-only components (no `commands`) declare a repo for the multi-repo invariant without contributing workflow steps. Workflow `type: command` step shapes: `{component, command}` | `{component, run}` | `{run}`; no `cwd:` field. Authoring patterns in [`defaults/workflow-authoring-guide.md`](defaults/workflow-authoring-guide.md). Full reference: [docs/internals.md — Multi-repo & components](docs/internals.md#multi-repo--components) and [Workflows are project-scoped only](docs/internals.md#workflows-are-project-scoped-only).
- **Cleanup remote branches on archive** → goal archive snapshots agent branch list before teardown then push-deletes via `deleteRemoteGoalBranches` in `server.ts`; session archive eagerly push-deletes merged `session/*` via `session-eager-branch-delete.ts` from `session-manager.ts::terminateSession`. All push-deletes gated by `shouldSkipRemotePush()` in `skills/git.ts`. See [docs/design/orphan-remote-branch-cleanup.md](docs/design/orphan-remote-branch-cleanup.md) and [docs/internals.md — Remote branch cleanup](docs/internals.md#remote-branch-cleanup).
- **Maintenance cleanup** → Settings → Maintenance. Scan/preview/execute pattern. Hosts Search Index panel. See [docs/internals.md — Semantic search](docs/internals.md#semantic-search).
- **Per-project settings** → Settings → project tab. Inherits from System. REST: `GET/PUT /api/projects/:id/config`. See [docs/internals.md — Per-project config](docs/internals.md#per-project-config).
- **Toggle the agent-finish notification beep** → Settings → General → "Play sound when an agent finishes". Preference key `playAgentFinishSound` in `preferences.json` (default ON when unset). Loaded on boot in `src/app/main.ts`, re-applied on `preferences_changed` in `src/app/remote-agent.ts`, written by Settings → General in `src/app/settings-page.ts`. Gate is a single guard at the top of `RemoteAgent.playNotificationBeep()` reading `document.documentElement.dataset.playAgentFinishSound`; favicon badge is independent of this toggle. Mirrors the existing `showTimestamps` preference end-to-end.
- **Native-YAML project.yaml fields** → `config_directories` and `sandbox_tokens` are stored as native YAML (not JSON-stringified). Read via typed accessors on `ProjectConfigStore` (`getConfigDirectories`, `getSandboxTokens`); legacy on-disk forms are parsed transparently and rewritten on next save. `PUT /api/projects/:id/config` rejects legacy JSON-string payloads for these keys with 400, and rejects the seven legacy top-level QA keys (`qa_start_command`, `qa_build_command`, `qa_health_check`, `qa_browser_entry`, `qa_env`, `qa_max_duration_minutes`, `qa_max_scenarios`) with a migration message pointing at `components[<name>].config[<key>]`. See [docs/internals.md — Native-YAML project.yaml fields](docs/internals.md#native-yaml-projectyaml-fields).
- **Mid-session project-config edits** → any agent can call `propose_project` (re-uses the same three-view proposal panel as the project assistant — see Add a project) against the current registered project; the user reviews a diff in a "Project" tab in the preview panel and accepts via `PUT /api/projects/:id/config` without ending the session. Key: `session-manager.ts::acceptProjectProposal` (dispatches provisional vs registered), `render.ts::projectProposalPanel`, `propose_project` tool, `PUT /api/projects/:id/config`. Spec: [docs/design/mid-session-project-proposals.md](docs/design/mid-session-project-proposals.md); summary in [docs/internals.md — Per-project config](docs/internals.md#per-project-config).
- **Edit a proposal mid-session / proposals as files** → every in-flight `propose_*` payload is mirrored to `.bobbit/state/proposal-drafts/<sessionId>/<type>.{md,yaml}` (markdown + YAML frontmatter for `goal`, native YAML for the other five). The agent uses `view_proposal(type)` and `edit_proposal(type, old_text, new_text)` (exact-string replacement, same semantics as the builtin `edit` tool) to surgically tweak a draft instead of re-emitting kilobytes of YAML. Failed edits roll back atomically (`.tmp` → rename) with structured error codes (`FILE_NOT_FOUND`, `OLD_TEXT_NOT_FOUND`, `OLD_TEXT_NOT_UNIQUE`, `FRONTMATTER_MALFORMED`, `YAML_PARSE_ERROR`, `MISSING_REQUIRED_FIELD`, `STRUCTURAL_VALIDATION_FAILED`). The six per-type proposal slots in client state are collapsed into one `state.activeProposals: Partial<Record<ProposalType, ProposalSlot>>` keyed by type; per-type behaviour (first-emit auto-select, streaming shallow-merge, accept) plugs in via `PROPOSAL_TYPE_REGISTRY` in `src/app/proposal-registry.ts`. Unified draft + dismissal helpers in `src/app/proposal-helpers.ts`. WS events `proposal_update {source: "edit"|"seed"|"rehydrate"|"restore", rev}` and `proposal_cleared` flow through the unified `remote.onProposal` callback in `session-manager.ts`; the legacy per-type `_checkToolProposals` streaming-partial path coexists (dual-fire). Per-session directory cleaned by `session-manager.ts::terminateSession`. Restart-survival: server enumerates and re-broadcasts on WS attach. **Revision snapshots:** every successful `propose_*` and `edit_proposal` write also writes an immutable per-rev snapshot under `<stateDir>/proposal-drafts/<sessionId>/<type>.history/<rev>.<ext>`; the server stamps a monotonic `rev: number` on every `proposal_update` (single source of truth — clients overwrite `slot.rev` with the server value, never client-increment). Both `ProposalRenderer` and the new `EditProposalRenderer` parse a `__proposal_rev_v1__:<n>` marker embedded in tool-result text and route their "Open proposal" button through `POST /api/sessions/:id/proposal/:type/restore` `{rev}` — which copies snapshot N back to the live draft AND writes a NEW snapshot at `currentRev+1`, so the rollback itself appears in the chat timeline (no silent data loss). Legacy archived sessions without the marker fall back to the original `{type, fields}` round-trip via the existing per-type callbacks. Each panel header shows a `rev N` badge. Why: users couldn't tell which revision was live, and clicking the *original* propose card after later edits silently destroyed every later edit by re-dispatching the original payload. Key: `src/server/proposals/proposal-files.ts` (atomic file API + `editProposalFile` rollback + `writeSnapshot` / `readSnapshot` / `latestRev` / `restoreSnapshot`), `src/server/proposals/proposal-types.ts` (per-type plugins: filename, serialize, parse, requiredFields), `src/server/server.ts` (REST), `src/server/ws/protocol.ts` (events), `defaults/tools/proposals/{view,edit}_proposal.yaml` + `extension.ts`, `src/ui/tools/renderers/{ProposalRenderer,EditProposalRenderer,proposal-rev-marker}.ts`. Specs: [docs/design/editable-proposals.md](docs/design/editable-proposals.md), [docs/design/proposal-revision-snapshots.md](docs/design/proposal-revision-snapshots.md); mechanism: [docs/internals.md — Editable proposals](docs/internals.md#editable-proposals); REST: [docs/rest-api.md — Proposal drafts](docs/rest-api.md#proposal-drafts).
- **Add QA testing** → set `qa_start_command` (and friends — `qa_health_check`, `qa_browser_entry`, `qa_max_duration_minutes`, `qa_max_scenarios`) on the relevant component's `config:` map in `project.yaml`. Inline env vars directly into `qa_start_command` (e.g. `PORT=$PORT NODE_ENV=test npm start`); there is no separate `qa_env` field. The `agent-qa` workflow step's `component:` field selects which component's `config` map the `/qa-test` skill reads. See [docs/qa-testing.md](docs/qa-testing.md). Screenshots: agent uses `<img src="file:///<path>">` from `[screenshot_file]` (NOT base64 — bloats transcript). Server inlines file:// refs to data URIs on report submission.
- **Add a verification reminder site (llm-review / QA / legacy RpcBridge)** → there are four such sites in `src/server/agent/verification-harness.ts` (`_tryResumeFromSession`, `runLlmReviewViaSession`, QA-tester reminder, legacy direct-`RpcBridge` reminder). After dispatching the reminder prompt and before racing the `verification_result` Promise against `SessionManager.waitForIdle`, await `SessionManager.waitForStreaming(sessionId, 10_000).catch(() => {})` so a resumed-from-idle session has a chance to enter the new turn. Without this, the synchronous-resolve behaviour of `waitForIdle` terminates the reviewer ~46ms after the prompt. Reviewer/QA sessions are registered via `TeamManager.registerReviewerSession()` and persisted with `kind: "reviewer"` so `resubscribeTeamEvents()` skips them on restart. See [docs/internals.md — Reminder race after restart-resume](docs/internals.md#reminder-race-after-restart-resume) and [docs/internals.md — Reviewer kind & restart resume](docs/internals.md#reviewer-kind--restart-resume).
- **Create a design mockup** → `/mockup` skill or [.bobbit/config/docs/design-mockups.md](.bobbit/config/docs/design-mockups.md).
- **Generate an image** → agent calls `generate_image` (`defaults/tools/images/generate_image.yaml` + `extension.ts`, sharing `defaults/tools/_shared/gateway.ts` for gateway URL/token resolution); gateway routes via `POST /api/image-generation/generate` to provider helpers in `src/server/agent/image-generation.ts`. Per-session model selection lives in the footer picker (`src/ui/dialogs/ImageModelSelector.ts`) → WS `set_image_model` (`src/server/ws/handler.ts`) → `imageModelProvider` + `imageModelId` on the session row. System default preference key `default.imageModel`, edited in Settings → Models → Image. Agent routing rules (DALL-E ids vs `openai/gpt-image-2` vs Google ids) live in `defaults/system-prompt.md`. See [docs/internals.md — Image generation routing](docs/internals.md#image-generation-routing) and [docs/rest-api.md — Image generation](docs/rest-api.md#image-generation).
- **Add / debug per-provider OAuth** → `src/server/auth/oauth.ts` (`oauthStartExternal`, `oauthComplete`, `oauthStatus`, `oauthFlowStatus`); REST endpoints `GET /api/oauth/status?provider=`, `POST /api/oauth/start`, `POST /api/oauth/complete`, `GET /api/oauth/flow-status?flowId=&provider=` (all keyed by `provider` — `anthropic` and `openai-codex`). UI lives in Settings → Account (`src/app/settings-page.ts`); the per-row Re-auth button uses the module-scoped `accountReauthing` flag in that file to disable siblings while one flow is in flight. See [docs/rest-api.md — OAuth](docs/rest-api.md#oauth).

## Debugging

See [docs/debugging.md](docs/debugging.md) for full checklists. Keyword index for common symptoms:

- **Session persistence** — `.bobbit/state/sessions.json`; missing `.jsonl` skips restore. Legacy persisted `session.poolId` / `session.worktreeDegraded` fields on disk are silently ignored on load (back-compat after the worktree-rename removal — see [docs/design/remove-session-worktree-rename.md](docs/design/remove-session-worktree-rename.md)).
- **`lastActivity` reads as "just now" after server restart / sessions cluster at identical timestamps** — three sites in `src/server/agent/session-manager.ts` install rpc-event listeners that bump `session.lastActivity`: `restoreSession` (~L2483-2500), the role-restart path (~L3539-3548), and the abort-restart path (~L4708-4720). All three now wrap the bump in `if (isUserVisibleActivity(event))`. The exported `isUserVisibleActivity` helper (single source of truth, top of `session-manager.ts`) accepts only `message_update`, `message_end`, `tool_execution_start`, `tool_execution_end`, `agent_end`. Lifecycle frames (`agent_start`, `agent_idle`, `connection_state`, `state`, `session_title`, `auto_compaction_*`, `process_exit`, container events, gate verification events) are filtered — these fire automatically post-resume and would otherwise clobber the pre-restart timestamp with `Date.now()`. The `restoring` / `switchingSession` flags remain for cost-tracking only; they are no longer relied on for `lastActivity`. The dormant-session path (`addDormantSession`) preserves `ps.lastActivity` directly. Test: `tests/session-restore-last-activity.test.ts`. See [docs/internals.md — Read/unread state](docs/internals.md#readunread-state) (`lastActivity` preservation across restart).
- **Sandbox status** — `GET /api/sandbox-status`; container label `bobbit-project=<projectId>`.
- **Stuck gate verification** — `POST /api/goals/:id/gates/:gateId/cancel-verification` or dashboard Cancel.
- **Worktree setup not running on pool / staff** — `components[*].worktreeSetupCommand` is the single source of truth. Pool `_fill()`, staff wake refresh, and session-setup all route through `runComponentSetups()` (`src/server/skills/worktree-setup.ts`). The legacy top-level `worktree_setup_command` is read only by `state-migration/migrate-project-yaml.ts`; reads outside that file are forbidden and guarded by a unit test in `tests/worktree-pool.test.ts`. A sibling guard in `tests/worktree-setup-fallback.test.ts` source-greps `src/` to ensure no caller passes a `setupCommand` to `createWorktree` / `createWorktreeSet` or references the deleted `setupWorktreeDeps` helper. Pool fills log `[worktree-pool] running setup for components: <names>`. See [docs/debugging.md — Worktree setup hook not running](docs/debugging.md#worktree-setup-hook-not-running) and [docs/internals.md — Session worktrees](docs/internals.md#session-worktrees).
- **Gate verification baselines** — pre-impl gates don't diff; impl+ diff against `origin/<primary>...HEAD`. See [docs/goals-workflows-tasks.md — Gate verification baselines](docs/goals-workflows-tasks.md#gate-verification-baselines).
- **Gate/task tool bloat** — use `?view=summary`; drill via `gate_inspect`. See [docs/rest-api.md — Summary views](docs/rest-api.md#summary-views-viewsummary).
- **Dead/ghost search results** — `ProjectContextManager.searchAll()` post-filters orphans. See [docs/internals.md — Orphan filtering & stale-click safety net](docs/internals.md#orphan-filtering--stale-click-safety-net).
- **Search index** — FlexSearch at `.bobbit/state/search.flex/`; delete to force rebuild. Meta mismatch auto-rebuilds. See [docs/internals.md — Semantic search](docs/internals.md#semantic-search).
- **Sidebar child loading** — check server BFS enrichment, archived-goals `archivedSessions`, on-demand fallback guard. See [docs/debugging.md](docs/debugging.md) "Paginated archives".
- **QA screenshot token bloat** — verify extension emits `[screenshot_file]` not `[screenshot_base64]`. See [docs/qa-testing.md — Screenshots in QA reports](docs/qa-testing.md#screenshots-in-qa-reports).
- **Stale project-proposal panel after second `propose_project`** — the three-view panel (Components / Workflows / Diff) must reflect the latest payload across repeated calls in the same session. Three bugs historically caused staleness: (A) structured `components`/`workflows` getting JSON-stringified into legacy field rows; (B) `onFieldInput` clobbering them with empty-string keystrokes; (C) `onProjectProposal` replacing rather than shallow-merging incoming partials. Fixes live in `src/app/session-manager.ts::onProjectProposal` (shallow-merge of `components`/`workflows` from the previous proposal when missing in the new payload, **plus per-component shallow-merge** so a partial re-emit of one component carries forward its prior `commands` / `config` maps) and `src/app/render.ts::projectProposalPanel` (`onFieldInput` early-returns for the two structured keys; structured side-tables flow through `projectComponentsView` / `projectWorkflowsView` / `projectDiffView` in `src/app/project-proposal-views.ts`, including a per-component `Config` table testid `component-config-${name}`). See [docs/debugging.md — Project proposal panel doesn't reflect the latest propose_project call](docs/debugging.md#project-proposal-panel-doesnt-reflect-the-latest-propose_project-call).
- **Monorepo subprojects not detected** — confirm `src/server/agent/monorepo-scan.ts` recognises the workspace manifest type (`pnpm-workspace.yaml`, `package.json` `workspaces`, `nx.json`, `turbo.json`, `lerna.json`, `Cargo.toml` `[workspace]`, `go.work`, Gradle `settings.gradle[.kts]` `include(...)`); output is capped at `MAX_CANDIDATES = 30` with an alphabetical truncation marker. See [docs/debugging.md — Monorepo subprojects not detected](docs/debugging.md#monorepo-subprojects-not-detected).
- **Legacy JSON-string `project.yaml` field rejected (HTTP 400)** — `PUT /api/projects/:id/config` rejects JSON-string payloads for `config_directories` and `sandbox_tokens` (send structured arrays of mappings) and rejects the seven legacy top-level QA keys (`qa_start_command`, `qa_build_command`, `qa_health_check`, `qa_browser_entry`, `qa_env`, `qa_max_duration_minutes`, `qa_max_scenarios`) entirely — they live on `components[<name>].config[<key>]` now. The on-disk YAML loader still tolerates legacy forms for back-compat (lazy migration on first boot moves them onto the relevant component's `config` map and inlines `qa_env` into `qa_start_command`); only the wire payload is strict. See [docs/debugging.md — Legacy JSON-string project.yaml field rejected](docs/debugging.md#legacy-json-string-projectyaml-field-rejected) and [docs/internals.md — Native-YAML project.yaml fields](docs/internals.md#native-yaml-projectyaml-fields).
- **Skill resource manifest (Level-3)** — SKILL.md `@path` markdown refs are no longer auto-inlined; agents read referenced files on demand. An activation header (HTML comment fence `<!-- skill-activation-header -->…<!-- /skill-activation-header -->`) is prepended to model-facing `expanded` with the skill root + one-level manifest of `references/`/`scripts/`/`assets/` (2 KB cap, alphabetical truncation). `SkillChip.displayBody` strips the header so the chat-bubble disclosure shows only the SKILL.md body the author wrote. Inside the Docker sandbox, built-in/personal skill roots aren't mounted → degraded header (no manifest); project-local skills work. Key files: `src/server/skills/skill-manifest.ts`, `resolve-skill-expansions.ts`, `slash-skills.ts`, `src/ui/components/SkillChip.ts`. See [docs/internals.md — Sandbox skill visibility](docs/internals.md#sandbox-skill-visibility) and [docs/debugging.md — Skill references not loading](docs/debugging.md#skill-references-not-loading).
- **Skill chip / autonomous activation** — user-typed `/name args` keeps its literal text in the chat bubble; resolved expansions ride alongside in `skillExpansions[]` (sidecar at `<stateDir>/skill-sidecar/<sessionId>.jsonl`) and render as `<skill-chip>` elements. Model-facing prompt is byte-equal to the legacy expanded form. Agent-side autonomous activation goes through `activate_skill` (tool group `Skills`, list capped at 4 KB in the system prompt). Old sessions with no sidecar render as plain text — backward compatible. Key files: `src/server/skills/resolve-skill-expansions.ts`, `src/server/skills/skill-sidecar.ts`, `src/server/ws/handler.ts` (must include `skillExpansions` in the user-message echo — regression risk), `src/server/agent/system-prompt.ts`, `defaults/tools/skills/`, `src/ui/components/SkillChip.ts`. See [docs/debugging.md — Skill chip not rendering](docs/debugging.md#skill-chip-not-rendering) and [docs/design/skill-ux-and-autonomous-activation.md](docs/design/skill-ux-and-autonomous-activation.md).
- **Tool-guard extension ParseError** — new sessions crash for roles with a `never`-policy tool; quoting slip in the template-literal generator in `tool-guard-extension.ts`. Guarded by `tests/tool-guard-extension.test.ts`. See [docs/debugging.md](docs/debugging.md).
- **Auto-nudge flooding** — `nudgePending` guard in TeamManager.
- **Reviewer session triggers spurious "Agent finished" team-lead nudge after restart** — `TeamAgent` / `PersistedTeamEntry.agents[].kind: "worker" | "reviewer"`; `TeamManager.registerReviewerSession` writes `"reviewer"`; `resubscribeTeamEvents()` skips reviewers on restart and `notifyTeamLead()` has the same defensive guard (with `role === "reviewer"` fallback for pre-`kind` persisted records). Restart-only bug — live path subscribes only to `tool_execution_end`. Key: `src/server/agent/team-manager.ts`, `src/server/agent/team-store.ts`. See [docs/debugging.md — Reviewer session triggers spurious team-lead nudge](docs/debugging.md#reviewer-session-triggers-spurious-agent-finished-team-lead-nudge-after-restart) and [docs/internals.md — Reviewer kind & restart resume](docs/internals.md#reviewer-kind--restart-resume).
- **Resumed reviewer terminated ~46ms after server restart, before reminder is acted on** — reminder race in `_tryResumeFromSession`: `SessionManager.waitForIdle` resolves synchronously on an already-idle session, so the harness declared failure before the agent ever saw the reminder prompt. Fix: new `SessionManager.waitForStreaming(sessionId, timeoutMs = 10_000)` (sibling of `waitForIdle`, resolves on `agent_start`) is awaited at all four reminder sites in `verification-harness.ts` before the existing `waitForIdle` race. Symmetric across `_tryResumeFromSession`, `runLlmReviewViaSession`, QA-tester reminder, legacy direct-`RpcBridge` reminder — any new reminder site must follow the pattern. See [docs/debugging.md — Resumed reviewer terminated ~46ms after restart](docs/debugging.md#resumed-reviewer-terminated-46ms-after-server-restart-before-reminder-is-acted-on) and [docs/internals.md — Reminder race after restart-resume](docs/internals.md#reminder-race-after-restart-resume).
- **Lesson 4.1 — duplicate child spawned on every restart / N+1 dupes after N restarts of an in-flight subgoal** — the harness's `createGoal` call in `runSubgoalStep` is followed by something other than a synchronous `updateGoal({ spawnedFromPlanId })`. Any await between the two persists a child with `spawnedFromPlanId === undefined`; on the next gate signal the tier-1 lookup misses and a fresh dupe is spawned. Tier-5 rescue (match by `(parentGoalId, title)` for `spawnedFromPlanId === undefined`) back-fills `spawnedFromPlanId` so future signals take the cheap path, but prevention is mandatory. Pinned by `tests/runSubgoalStep-spawn-stamps-planId.test.ts`. Apply at every spawn site (harness, `goal_spawn_child` REST, any future spawner). See [docs/nested-goals.md](docs/nested-goals.md).
- **Lesson 4.2 — subgoal step waits forever on a child that's been archived (cached pointer to a dead row)** — `active.steps[i].subgoal.childGoalId` points at an `archived && state !== "complete"` record. The existing `archived && state === "complete"` short-circuit doesn't fire because the child's `state` is `in-progress`/`shelved`. Fix: after tier-1/1.5 lookup, sanity-check the resolved child; if `archived && state !== "complete"` → wipe the cached entry on both `active.steps[i].subgoal` AND the persisted gate step's subgoal, fall through to tier-3 spawn. `GoalManager.archiveGoalAfterMerge` stamps `state: "complete"` BEFORE flipping `archived: true` so a crash mid-archive can't reproduce this. Backfill: `GoalManager.backfillCompleteState(gateStore)` runs once per project on boot to flip every archived goal whose `ready-to-merge` is `passed`. Pinned by `tests/runSubgoalStep-stale-archived-invalidates.test.ts`, `tests/goal-manager-backfill-state-complete.test.ts`.
- **Lesson 4.3 — child goal created with `workflow: undefined` / gates never appear / `ready-to-merge` polls forever** — `GoalManager` was constructed without a `WorkflowStore`. `createGoal({workflowId: …})` now throws fail-loud when `workflowId` is given but neither `resolvedWorkflow` nor `workflowStore` is available. Fix: `project-context.ts` constructs `new GoalManager(goalStore, workflowStore)` with `WorkflowStore` built before `GoalManager`. Legacy paths preserved: `createGoal({})` (no workflowId, no workflowStore) and `createGoal({workflowId, resolvedWorkflow})` both still succeed for assistant sessions and test fixtures. Pinned by `tests/goal-manager-workflow-store-required.test.ts`.
- **Lesson 4.4 — workflow-less complete child stuck (`state=complete, archived=null, workflow=null`)** — pre-fix children created without a workflow are unreachable from `ready-to-merge`. `runSubgoalStep` includes a recovery branch with the conjunctive predicate `state === "complete" && !archived && !workflow`: try `mergeChild` directly; on success run `teardownTeam + archiveGoalAfterMerge`, on `alreadyMerged` same archive flow, on conflict the step fails with a manual-recovery directive. Predicate must stay narrow (all three conditions) — broader matches risk archiving live work. Pinned by `tests/runSubgoalStep-degenerate-workflow-less-recovery.test.ts`.
- **Lesson 4.6 — phantom gate failure after gateway restart, every step output reads "Step was running but had no session ID — cannot resume after restart"** — restart-interrupt suppression. `_resumeOneVerification` consults `shouldSuppressRestartInterrupt` from `src/server/agent/verification-logic.ts`; when every failed step matches `RESTART_INTERRUPT_MARKERS` (or is an empty-output llm-review/agent-qa step), the gate is left `pending` rather than `failed` and the team-lead receives a benign "interrupted by restart, please re-signal" notification. Real failures still mark the gate failed — the predicate is conjunctive, every failed step must match. Add new markers by appending to `RESTART_INTERRUPT_MARKERS` and updating `tests/restart-interrupt-suppression.test.ts`.
- **Lesson 4.7 — resumed reviewer goes idle without calling `verification_result` after a restart, "Agent did not call verification_result after server restart and reminder"** — the resumed reviewer is a fresh subprocess; the terse legacy reminder arrives without the original task spec. Fix: this string is now in `TRANSIENT_ERROR_PATTERNS` so the resume path's transient-detection branch promotes recovery to `_rerunLlmReviewStep`, which rebuilds the kickoff with full context. Belt-and-braces: the same string is also in `RESTART_INTERRUPT_MARKERS`, so if rerun is unavailable the gate stays `pending` rather than `failed`.
- **Lesson 4.8 — live-path reviewer emits its verdict as chat-text and ends turn instead of calling `verification_result`** — the two-sentence legacy reminder didn't elicit a tool call. Use `buildContextRichReminder(originalKickoff)` from `src/server/agent/verification-harness.ts`: leads with `## STOP — verification_result not called`, says any chat-text verdict is INVISIBLE to the gate, tells the agent to call the tool with whatever opinion it ALREADY FORMED, then re-attaches the FULL original kickoff after a `---` separator. Wired into both LLM-review and agent-QA live paths; the resume path keeps the legacy terse reminder (no kickoff to rebuild).
- **Lesson 4.9 — WS-acked prompt never reaches the agent after restart, throws "Agent process not running" inside enqueuePrompt** — the persisted session's record was restored but its in-process RPC bridge was dead. Fix: `SessionManager._dispatchPromptWithReviveOnDeadBridge` checks `rpcClient.running` at the two new-prompt sites in `enqueuePrompt` (error-recovery branch + idle+empty branch); if false, calls `restartAgent`, re-resolves the SessionInfo, and dispatches on the fresh bridge. Steady-state retry/drain paths stay loud — only the new-prompt sites auto-revive, so a real bridge death surfaces in logs.
- **Lesson 4.10 — "Restart Agent" button keeps re-throwing on a session that has neither an `agentSessionFile` nor a `role` (zombie session)** — a coder spawn raced against a chaotic teardown; the agent died before `agentSessionFile` was persisted. Fix: `restartAgent` sanity-checks the persisted record before calling `restoreSession`. If `!agentSessionFile && !role` it's an unrecoverable zombie — archive the row (`store.update(id, {archived: true, archivedAt: Date.now()})`) and throw a structured error with `code: "SESSION_UNRECOVERABLE_ARCHIVED"`. The UI surfaces this code with an actionable message instead of opaquely re-emitting "Agent process not running" on every click.
- **Lesson 4.11 — endless restart loop on boot ("server crashed boot, harness restarted in 1s, …")** — three-part fix in the harness + team-manager. (1) `harness.ts` crash-loop guard tracks `consecutiveQuickCrashes` against `HEALTHY_UPTIME_MS = 10s`; after `CRASH_LOOP_THRESHOLD = 5` quick crashes the harness logs `Run "npm run restart-server" to resume after fixing the root cause` and stops auto-restarting (manual restart trigger clears the counter). (2) `TeamManager.restoreTeams` orphan-team-store cleanup walks every persisted team entry FIRST and drops entries whose `goalId` is not in the owning project's goal store, logging `Cleaned N orphan team entries on boot.` (3) `TeamManager.resubscribeTeamEvents` zombie-reviewer sweep wraps `unregisterReviewerSession` in try/catch with `console.error` + continue-on-error semantics so one bad reviewer can't take down boot.
- **Lesson 4.12 — sessionless in-progress goal stranded after multiple restarts (no team agents, no team-lead session)** — the harness's existing recovery only fires when there's an active verification with the child's `planId`, but the parent's verification record was itself lost in the restart. Fix: `TeamManager._bootRespawnSessionlessGoals` runs at the END of `resubscribeTeamEvents` and walks every non-archived `state: in-progress, setupStatus: ready, team: true` goal whose `goalId` has no entry in `this.teams`, calling `setupWorktreeAndStartTeam` to respawn a fresh team-lead. Each respawn is wrapped in try/catch. Logs `Boot recovery: respawning team-lead for sessionless in-progress goal "<title>"`.
- **Lesson 4.13 — parent team-lead idle for hours despite a paused child being its only "in-flight" descendant** — `anyInFlightChild` walked goals and found the paused child with `state="in-progress", archived=false`, returned `true`, and skip-suppressed the parent's idle nudge forever. Fix: pure helper `src/server/agent/team-manager-helpers.ts::anyInFlightChild` excludes children where `paused === true`. A paused child can't make progress on its own — only the parent (or user) can act. Mixed-progress preserved: a paused sibling alongside an active one still counts as in-flight via the active one.
- **Lesson 4.18 — generic "Check verification output" notification, no failed-step name or output, no merge-gap diagnostic** — `notifyTeamLead` was emitting a single-line message. Fix: `buildVerificationFailureMessage(gateId, steps, goalBranch?)` in `src/server/agent/notify-team-lead-failure.ts` includes the failed step name(s), the first failed step's truncated output (max 600 chars), and a merge-gap diagnostic when `failed.some(s => s.type === "command")` (concrete `git log --oneline -5 <branch>` and `git for-each-ref` commands). Four call sites pass `{steps, goalBranch}`: cached-skip-all-steps short-circuit, live `verifyGateSignal` end branch, live catch-block, resume-error catch-block. The fifth (resume-suppressed-by-restart) intentionally bypasses the builder. Tests: `tests/notify-team-lead-failure-detail.test.ts`.
- **Lesson 4.19 — Plan tab shows a child as "failed" when there's a successful merged sibling (zombie sibling shadows the real success)** — naïve "live > archived" tie-break shadowed the merged-and-archived original. Fix: `resolvePlanStepChild` uses success-aware tiers — live in-progress > archived complete > live other > archived non-complete; tie-breaks within a tier are most-recent `createdAt`. Apply both server-side (`src/server/agent/verification-harness.ts::resolvePlanStepChild`) AND client-side (`src/app/plan-node-state.ts::resolvePlanNodeChild`) — the two MUST agree on display state for any input. Pinned by `tests/runSubgoalStep-tier-resolution.test.ts`, `tests/plan-node-state.test.ts`.
- **Lesson 4.20 — Plan tab missing on goals with ad-hoc subgoals (no formal plan)** — only goals with a `goal-plan` gate had a Plan tab. Fix: `src/app/plan-synthesis.ts::buildPlanSteps` synthesises a "living plan" from non-archived children, clustering by `createdAt` timestamp gap (default 60s) into phases. `shouldShowPlanTab(goal, goals?)` in `src/app/goal-dashboard-tab-visibility.ts` returns true if EITHER the workflow has a `goal-plan` gate OR the goal has any non-archived children. Synthesis recomputes from the live goal tree on every render, so the Plan tab is a living document. Goals with formal plans + ad-hoc children get the formal plan plus orphans appended past the formal max phase.
- **Lesson 4.21 — tree cost rollup shows only the root team-lead's cost, not the descendants' accumulated cost** — dashboard rendered per-goal cost only. Fix: `GET /api/goals/:id/tree-cost` walks the descendant tree (BFS, depth cap 32) via the `rootGoalId` chain and sums each goal's cost. Handler resolves the rollup root as `goal.rootGoalId ?? goal.id` so a request against a child still returns the WHOLE-TREE rollup. Pure helper: `computeTreeCost(rootGoalId, allGoals, costTracker, sessionIdsForGoal)` in `src/server/agent/cost-tracker.ts`. Cache lives on the existing `CostTracker` (per-tracker WeakMap keyed by `rootGoalId`, invalidated when `costTracker.getGeneration()` ticks past the cached generation). Generation bumps on every `recordUsage`/`removeSession`, so cache invalidation is free. Breakdown sorted `depth ASC, createdAt ASC`; archived descendants still counted; cross-tree goals excluded by the filter `g.id === rootGoalId || g.rootGoalId === rootGoalId`. Tests: `tests/tree-cost-rollup.test.ts`, `tests/api-goals-tree-cost.test.ts`.
- **Lesson 4.22 — Plan tab shows only direct children, grandchildren invisible** — the Plan tab DAG SVG used to require clicking into a child to see its plan. Fix: each plan node card has a chevron disclosure; click expands and renders the child's plan inline (recursive). Render depth cap 3 in the SVG (deeper trees collapse with "Show N more…" — sidebar's depth-5 cap is a separate constant via `buildNestedGoalForest`'s `maxDepth`). Live updates subscribe to `goal_state_changed` and `goal_child_spawned` WS events on every visible goal in the tree; recompute the synthesis on each event with a 250ms trailing-edge throttle to avoid layout thrash.
- **Child team-lead raises a PR even though it shouldn't / `gh pr create` runs on a child goal** — Stanza B (rendered when `parent !== undefined`) must contain the literal `**DO NOT raise a PR.**` line. Confirm the session-manager populated `parts.nestingContext.parent` when assembling the prompt; if `parent` is undefined for a child goal the builder falls through to Stanza A and the agent receives the *root* mandate ("raise the PR"). Cross-check `goal.parentGoalId` is set on disk (Phase 1 lazy migration only fills `rootGoalId` for child goals; `parentGoalId` is the trigger for Stanza B). Source: `src/server/agent/system-prompt.ts::buildNestingContextSection`. Diagnostic: `GET /api/sessions/:id/prompt-sections` and look for a section labeled `Goal Nesting`.
- **`PATCH /api/goals/:id/plan` returns 409 (or 400)** — diagnostic order: (1) **400 `NO_EXECUTION_GATE`** — the goal's snapshotted workflow has no gate with `id: "execution"`. The check is gate-based, not workflow-id-based: any workflow whose snapshot contains an `execution` gate can hold a subgoal plan. The canonical `parent` workflow (charter → plan-review → goal-plan → execution → integration → ready-to-merge) qualifies, but a custom workflow with an `execution` gate works too. Fix: re-create the goal with the `parent` workflow, or call `goal_spawn_child` directly per step. (2) **409 `CRITERIA_DROP`** — an acceptance criterion is missing from the union of `{rootSpec, proposed step specs}`; inspect `uncoveredCriteria` and quote the criterion verbatim into the relevant subgoal's spec (whitespace-normalised, case-insensitive substring match). (3) **409 `RESTRUCTURE_REQUIRES_PAUSE`** — a step was removed (or an existing step's phase decreased) on a non-paused goal; pause the goal first via `POST /api/goals/:id/pause {cascade}` and re-PATCH. (4) Otherwise, this is a malformed `proposedSteps[]` payload (400 from the array validator). Source: the gate-detection guard lives in `src/server/server.ts` (PATCH plan handler — replaced the historical `workflowId !== "parent"` string check; commit `0f7e6a64`). See [docs/nested-goals.md — Mutation classifier](docs/nested-goals.md#mutation-classifier).
- **`x-opencode-session` header missing or shared across sessions** — diagnostic order: (1) confirm `~/.bobbit/agent/models.json` has the provider-level `headers` block on the `aigw` entry (regenerated by `writeAigwModelsJson` on next aigw configure/refresh if absent — and, since the startup-refresh change, regenerated automatically on every gateway startup when aigw is reachable, via `startupAigwCheck` in `src/server/agent/aigw-manager.ts`); (2) confirm the agent subprocess env has `BOBBIT_SESSION_ID` set to the Bobbit session id (each spawn path injects it — distinct per session); (3) if the value appears as the literal string `BOBBIT_SESSION_ID`, the resolver fell through to the plain-string branch instead of the `!cmd` branch — the `!` prefix should make this unreachable, so check the JSON quoting in `writeAigwModelsJson` first. The pi resolver caches `!cmd` results in a per-process `commandResultCache` keyed by the full command, but each Bobbit session spawns its own agent subprocess so caches never cross sessions. Bedrock/Claude requests legitimately carry no header — `bedrock-converse-stream` ignores `model.headers`. See [docs/internals.md — AI Gateway per-session header](docs/internals.md#ai-gateway-per-session-header-x-opencode-session).
- **`models.json` stale after gateway upgrade** — new gateway-side models or a missing `x-opencode-session` header block should resolve themselves on the next gateway restart: `startupAigwCheck` in `src/server/agent/aigw-manager.ts` re-discovers models and rewrites `~/.bobbit/agent/models.json` (preserving non-aigw providers and user `modelOverrides`). If the gateway is unreachable at startup, a `[aigw] gateway unreachable on startup` warning is logged and the existing file is left untouched — staleness is preferred to wiping a working file. `BOBBIT_SKIP_AIGW_DISCOVERY=1` skips only the network call (Bedrock env vars are still applied) — useful for offline dev. See [docs/internals.md — AI Gateway per-session header](docs/internals.md#ai-gateway-per-session-header-x-opencode-session).
- **Review/naming models under AI Gateway** — reviewer/QA bind via `applyReviewModelOverrides` (`src/server/agent/review-model-override.ts`); any failure throws → hard gate failure (no silent fallback). A role-level `model` field, when set, takes precedence over `default.reviewModel` for verification sub-sessions of that role (bound via the sibling `applyModelString` helper from `verification-harness.ts`). Naming model is unaffected and auto-picks cheapest-tier Claude via `pickFallbackAigwNamingModel` when no explicit pref. Settings → Models shows red "Unavailable" badge for stale prefs + per-row Test button (`POST /api/models/test`).
- **Live-steer lost after Stop** — must route through `SessionManager.deliverLiveSteer()`, which persists to `promptQueue` before calling `rpcClient.steer()`. Repro: `PI-25b`/`PI-25c` in `tests/e2e/abort-status-e2e.spec.ts`.
- **Session wedged after errored turn** — a new prompt/steer to a session with `lastTurnErrored=true` now implicitly unsticks it (clears the flag, prepends a `[SYSTEM: previous turn failed…]` prefix, dispatches fresh intent — no retry of the failed turn). Capped at `MAX_CONSECUTIVE_ERROR_TURNS = 3` so a persistently broken upstream can't be re-triggered on every nudge; beyond the cap, messages park until a human clicks Retry (which bypasses the cap). TeamManager no longer suppresses nudges to errored team leads. See [docs/debugging.md — Session wedged after errored turn](docs/debugging.md#session-wedged-after-errored-turn).
- **`bash_bg wait` not interrupted by steer** — steers must go via `deliverLiveSteer`; `BgProcessManager.waits` registry aborted by `abortAllWaits()`. See [docs/internals.md — Steer-interruptible bash_bg wait](docs/internals.md#steer-interruptible-bash_bg-wait).
- **Large file writes freezing** — `truncateLargeToolContent()` at >32KB. If lazy-load fails, check `.jsonl` exists.
- **Preview Open button broken** — `tool_result` must include the `__preview_snapshot_v1__` marker block; truncated snapshots hydrate via `GET /api/sessions/:id/tool-content/:mi/:bi`; flip panel via PATCH `{preview:true}` → POST `/api/preview`.
- **Streaming dedup/reorder** — events carry `seq`+`ts`; client sends `{type:"resume", fromSeq}` on reconnect; `resume_gap` falls back to `get_messages`. The BOBBIT_E2E-only `replay-buffered-events` test endpoint paces sends via `paceAndSend()` (see entry below). See [docs/design/streaming-dedup-reorder.md](docs/design/streaming-dedup-reorder.md).
- **`replay-buffered-events` flake / 4 MiB terminate during E2E (ST-DEDUP-01)** — the BOBBIT_E2E-only replay endpoint in `src/server/server.ts` paces `client.send()` via `paceAndSend()` from `src/server/replay-pacing.ts` (256 KiB buffered threshold, 2 s deadline) so it doesn't trip the production `broadcast()` 4 MiB overflow guard mid-test. See ST-DEDUP-01 in `tests/e2e/ui/stories-streaming.spec.ts` and [docs/design/streaming-dedup-reorder.md — Test-only replay pacing](docs/design/streaming-dedup-reorder.md#test-only-replay-pacing).
- **WS overflow guard — transient-spike tolerance** — production `broadcast()` in `src/server/agent/session-manager.ts` no longer terminates a client the instant `bufferedAmount` crosses 4 MiB. Decision logic lives in `src/server/ws-overflow-guard.ts::decideOverflowAction`: first observation over the threshold returns `send-and-defer-check`, which schedules a 10 ms re-check; only if the spike persists does the deferred re-check terminate the client. The original send still goes through, so a transient kernel-buffer spike (Windows + Playwright workers=3) doesn't lose a frame. Guarded by `tests/ws-overflow-guard.test.ts`.
- **Verification log Nx duplication** — streaming verification output (`<verification-output-modal>`, `<gate-verification-live>`) printing each line N× (where N tracks open sessions per goal in the tab, +1 per dashboard mount). Cause: every `RemoteAgent` owns its own session WS and `broadcastToGoal` fan-outs the same `gate_verification_*` event to all of them; each used to dispatch `gate-verification-event` on `document` independently. Fix: server stamps a monotonic `seq` on every gate verification event (`verification-harness.ts`, `ws/protocol.ts`); all dispatchers (`remote-agent.ts`, `goal-dashboard.ts`) funnel through `src/app/verification-event-bus.ts`, which dedupes by `(eventType, signalId, stepIndex, seq)` with a bounded LRU set. Listener-side, `VerificationOutputModal` + `GateVerificationLive` use `AbortController` so Lit re-renders/teardown can't leak duplicate listeners, and the modal tracks a bootstrap high-water `seq` to drop live events ≤ that mark when reopened mid-stream. See [docs/internals.md — Verification event dedupe](docs/internals.md#verification-event-dedupe) and [docs/debugging.md — Verification log duplicated Nx](docs/debugging.md#verification-log-duplicated-nx).
- **Scroll snaps back / vibration in idle session / tail loss on session-load / no re-engage affordance** — `AgentInterface` (`src/ui/components/AgentInterface.ts`) ResizeObserver only auto-scrolls on `delta > 0`; user intent is detected via explicit `wheel`/`touchstart`/`keydown` listeners and a programmatic-scroll echo filter (captured `scrollTop`/`scrollHeight` with sub-pixel tolerance `< 1` for HiDPI rounding), never timer-based heuristics. 10px stick-to-bottom tail (widened from 5px to absorb HiDPI rounding without losing tail). `_wasAtBottomAtLastUserScroll` carry-over (set in `_handleScroll` to the *current* `_stickToBottom`, cleared on `_handleUserIntent`/`_handleScrollKeydown`) is consulted in the `state_update` branch of `setupSessionSubscription` so a transient scroll-up that flips `_stickToBottom = false` between events doesn't lose the bottom. Session-load settle window (`_settleWindowActive` + `_settleWindowDeadline`, 2000ms, re-armed on every `setupSessionSubscription` call) re-scrolls on each ResizeObserver tick while sticky, exits on stable `scrollHeight` across two consecutive ticks / deadline / any user intent; torn down in `disconnectedCallback`. Jump-to-bottom button (`_showJumpToBottom`, recomputed in `_handleScroll`) appears when `scrollHeight - scrollTop - clientHeight > clientHeight * 0.5`; click runs `_scrollToBottom()` and sets `_stickToBottom = true` (no parallel flag). Tests: `tests/agent-interface-scroll.spec.ts`, `tests/agent-interface-scroll-hardening.spec.ts`, `tests/e2e/ui/jump-to-bottom.spec.ts`. See [docs/debugging.md — Scroll snap-back / vibration](docs/debugging.md#scroll-snap-back--vibration-in-idle-session) and [docs/internals.md — Chat scroll lock invariant](docs/internals.md#chat-scroll-lock-invariant).
- **Proposal panel button enabled mid-stream / scroll resets on delta** — streaming flag is `state.proposalStreamingByTag[<tag>_proposal]`, writer is `RemoteAgent._checkToolProposals` (cleared on the `_processedProposalIds.add(blockId)` branch + bulk-cleared on `agent_end` / `reset()`); panels in `src/app/render.ts` OR-merge the flag into submit `disabled` and render `streamingBadge()` + `STREAMING_BORDER`; scroll/selection preserved across Lit re-renders via `reconcileFollowTail` in `src/app/follow-tail.ts` (WeakMap-keyed, 5px tail, same three rules as chat scroll lock). See [docs/debugging.md — Proposal panel button enabled mid-stream](docs/debugging.md#proposal-panel-button-enabled-mid-stream--scroll-resets-on-delta) and [docs/internals.md — Proposal panel scroll lock invariant](docs/internals.md#proposal-panel-scroll-lock-invariant).
- **"Open proposal" on an old propose / edit card doesn't restore the panel** — diagnostic chain: (1) confirm the tool-result text on the card contains a `__proposal_rev_v1__:<n>` marker; if absent, this is a legacy archived session without snapshots — the renderer falls back to the `{type, fields}` round-trip and clicking the original propose card *will* destroy any later edits (expected, by design). (2) Check the WS frame in devtools Network → WS for a `proposal_update {source: "restore", rev: <newRev>}` after the click; if absent, the REST call failed — check the network tab for `POST /api/sessions/:id/proposal/:type/restore` and look for `404 {code:"SNAPSHOT_NOT_FOUND"}` (the snapshot file is missing). (3) Confirm `.bobbit/state/proposal-drafts/<sessionId>/<type>.history/` exists and contains `<rev>.<ext>` for the rev the card claims; if the dir is missing, this is a legacy session that predates the snapshot system and the live draft was written without history (the `latestRev = 0` fallback applies). (4) Confirm `slot.rev` in `state.activeProposals[type]` is being overwritten by the server-stamped value, not client-incremented — the panel header `rev N` badge should match the latest snapshot file. Key: `src/server/proposals/proposal-files.ts::restoreSnapshot` (atomic write + new-rev bump), `src/ui/tools/renderers/proposal-rev-marker.ts` (`parseRevFromResult`), `src/app/session-manager.ts::proposalOpenHandler` (rev branch vs legacy fields branch). See [docs/design/proposal-revision-snapshots.md](docs/design/proposal-revision-snapshots.md) and [docs/internals.md — Editable proposals](docs/internals.md#editable-proposals).
- **Proposal panel doesn't update after `edit_proposal` / proposal file rolled back** — diagnostic chain: (1) confirm `.bobbit/state/proposal-drafts/<sessionId>/<type>.{md,yaml}` exists on disk (per-session dir, type = `goal|project|workflow|role|tool|staff`); if missing, the agent never called `propose_<type>` first → `view_proposal` returns `404 {code:"FILE_NOT_FOUND"}`. (2) Check the WS frame in browser devtools Network → WS for a `proposal_update` event with `proposalType` matching the edit; if absent, the edit failed server-side and the file is unchanged. (3) `400 {code: "YAML_PARSE_ERROR" | "MISSING_REQUIRED_FIELD" | "STRUCTURAL_VALIDATION_FAILED" | "FRONTMATTER_MALFORMED"}` means `editProposalFile` parsed the post-edit content, validation rejected it, and the atomic `.tmp` was unlinked — file on disk is byte-for-byte identical to pre-edit. (4) `400 {code: "OLD_TEXT_NOT_FOUND" | "OLD_TEXT_NOT_UNIQUE"}` is an exact-match failure before any file write — `old_text` must match a single occurrence verbatim (whitespace-sensitive). (5) On WS reattach after a server restart, the handler enumerates the per-session dir and re-emits `proposal_update {source: "rehydrate"}` for each surviving file; if the panel doesn't reappear, check the unified `remote.onProposal` callback in `src/app/session-manager.ts` is wired and `state.activeProposals[type]` is being populated. Key: `src/server/proposals/proposal-files.ts::editProposalFile` (atomic write + rollback), `src/server/proposals/proposal-types.ts` (per-type validators), `src/server/ws/handler.ts` (rehydrate), `src/app/proposal-registry.ts` (`PROPOSAL_TYPE_REGISTRY`). See [docs/internals.md — Editable proposals](docs/internals.md#editable-proposals) and [docs/design/editable-proposals.md](docs/design/editable-proposals.md).
- **Stale messages trailing after newer ones on session navigate** — handled by the unified reducer in `src/app/message-reducer.ts`: snapshot rows get `_order = SNAPSHOT_ORDER_FLOOR + i` (strictly less than any live `seq`), the reducer sorts once by `(_order, _insertionTick)` per `apply()`, and the `snapshot` action is authoritative for any id it contains (id-match drop, with `"Context compacted"` text-prefix fallback for the synthetic compaction marker). See [docs/debugging.md — Stale messages on session navigate](docs/debugging.md#stale-messages-trailing-after-newer-ones-on-session-navigate) and [docs/internals.md — Reducer ordering invariant](docs/internals.md#reducer-ordering-invariant).
- **`bash_bg.wait` (or any tool-call) card renders twice with two independent timers, persists for the entire wait** — `message_end` for an assistant message with tool calls arrived without a string `id` (undefined / null / numeric / `0` / `""`), so the old `typeof msg.id === "string" ? msg.id : undefined` check demoted `streamingMessageId` to `undefined`, the `!streamingMessageId` short-circuit in `AgentInterface.ts`'s visible-messages filter opened, and the message rendered in both `MessageList` and `StreamingMessageContainer`. Two `<bg-process-renderer>` instances mean two `callStartByParams` WeakMap entries → two `Date.now()` start times → two diverging timers. The duplicate is normally cleared by the next event, but a parked `bash_bg.wait` produces no further events until it resolves. Fix lives in `src/app/streaming-message-id.ts::computeStreamingMessageId` (synthetic-id fallback `synth:tc:<firstToolCallId>`), called at both sites in `src/app/remote-agent.ts` (the `streamingMessageId` assignment and the reducer entry's `id` stamp — they must agree), with a belt-and-braces `state.streamingMessage` reference-equality guard in `src/ui/components/AgentInterface.ts`. Regression tests: `tests/dual-render-noid-message.test.ts` (5 cases incl. id=undefined/null/numeric), `tests/message-reducer.test.ts`, `tests/e2e/ui/bg-wait-no-dup.spec.ts`, `tests/e2e/ui/transcript-fidelity.spec.ts` (generic live-vs-refresh DOM-equality invariant under `STREAM_BURST:3` — catches this bug class without needing a targeted repro). Refactoring `BgProcessRenderer.getCallStart` to key on `bgId` instead of params object identity is a deliberate follow-up — not part of this fix. See [docs/internals.md — Streaming message id (synthetic fallback)](docs/internals.md#streaming-message-id-synthetic-fallback).
- **`StreamingMessageContainer.setMessage` drops one streaming delta after immediate clear** — `_immediateUpdate` flag in `src/ui/components/StreamingMessageContainer.ts::setMessage` is set in the immediate-clear branch (`immediate === true || message === null`) to disarm any in-flight rAF scheduled by a previous batched `setMessage`. Historically it was *only* cleared inside the rAF callback, so the immediate-clear branch returned with the flag sticky-true; the next batched `setMessage(msg, false)` scheduled a fresh rAF, the rAF saw `_immediateUpdate === true`, silently skipped its `_message = pending` assignment, and only then cleared the flag — one streaming delta lost per immediate clear. Symptom: streaming preview lags one delta behind reality after every toolResult clear or session navigate; with sparse deltas (slow toolCall input) the missing delta can be visible for hundreds of ms. Invariant: the flag's only job is disarming an in-flight rAF; once `_updateScheduled = false` has done that in the same branch, the flag has no further purpose and must be cleared synchronously before returning. Test: `tests/streaming-message-container-set-message.spec.ts`.
- **bash_bg.wait toolResult / toolCall-bearing assistant card duplicated after snapshot replay** — distinct from the in-flight dup above (which is a render-time filter bug). Symptom: live DOM has N+1 messages, post-refresh snapshot has N, the dup is a parked-or-completed `bash_bg.wait` toolResult or its parent assistant message. Cause: `live-event` ingests a `message_end` whose `id` is not a string (real LLM toolResult rows often omit `id`; mock-agent toolResults always do), the row enters `state.messages` un-id'd, then a server snapshot arrives with the canonical id'd copy. The snapshot survivor filter in `src/app/message-reducer.ts` previously only dropped live server-origin rows whose **string id** appeared in the snapshot — un-id'd live rows passed through and the snapshot's id'd copy was added on top. Fix: the survivor filter now also drops live server-origin `toolResult` rows whose `toolCallId` matches a snapshot row's `toolCallId`, and live server-origin `assistant` rows containing a `toolCall` whose `id` matches a snapshot assistant row's toolCall id (broader equivalence than id-only string match). The server snapshot is authoritative for any toolCall it contains. Test: `tests/dual-render-bg3.test.ts`; also covered by the generic live-vs-refresh DOM-equality invariant in `tests/e2e/ui/transcript-fidelity.spec.ts`. Investigation note: `tests/notes/bug2-investigation.md`. See [docs/internals.md — Reducer ordering invariant](docs/internals.md#reducer-ordering-invariant) and [docs/design/unified-message-ordering-reducer.md](docs/design/unified-message-ordering-reducer.md).
- **Out-of-order or duplicated messages in chat (proposals / `ask_user_choices` widgets appearing in wrong order, vanishing, or doubled)** — fix lives in the unified reducer in `src/app/message-reducer.ts`; the eight pre-reducer mechanisms (`_deferredAssistantMessage`, `_liveEventMessages`, `_pendingPermissionCards`, `_compactionSyntheticMessages`, `flushDeferredMessage`, optimistic-text dedupe, snapshot-merge stable sort, index-based render keys) are gone. Every transcript-mutating path is one of 13 reducer actions; the only sort key is `(_order, _insertionTick)`. Streaming preview duplicate suppression is render-time (`AgentInterface.renderMessages` filters `id === streamingMessage?.id`). See [docs/debugging.md — Out-of-order proposal / `ask_user_choices` widgets](docs/debugging.md#out-of-order-proposal--ask_user_choices-widgets) and [docs/internals.md — Reducer ordering invariant](docs/internals.md#reducer-ordering-invariant).
- **Continue-Archived button missing** — only renders when archived AND no `goalId` AND no `delegateOf` AND project still registered.
- **Continued session missing earlier transcript / agent CLI fails to rehydrate** — confirm the cloned `.jsonl` exists at the new session's `agentSessionFile` path (`<globalAgentDir()>/sessions/--<cwd-slug>--/<isoTs>_<uuid>.jsonl`) and that the row was persisted before spawn (so a hard kill can't strand the clone). `CrossRealmCopyError` surfaces as 422 — host↔sandbox and cross-project copies are rejected by design; the user must re-register the project with matching sandbox config. Tool-content lazy-loaded blocks live in `<stateDir>/tool-content/<sessionId>/`; the directory copy is best-effort and silently skipped when absent (no on-disk cache exists today — the agent CLI serves lazy-load requests from the cloned JSONL). See `src/server/agent/continue-archived.ts`, `src/server/agent/session-fs.ts::sessionFileCopy`, [docs/design/lossless-continue-archived.md](docs/design/lossless-continue-archived.md).
  - **Worktree-backed source → new session opens read-only with no editor.** The continue handler computes `destJsonl` against `proj.rootPath` but the agent CLI for a worktree session boots with `cwd = offsetCwd`, and `formatAgentSessionFilePath` embeds a `slugify(cwd)` segment in the path. If the clone is left under the project-root slug-dir, the agent CLI's `switch_session` fails to find the file, `handleSetupFailure` archives the new session, and the UI presents it as read-only. Fix lives in `src/server/agent/session-setup.ts::executeWorktreeAsync`: after `plan.cwd` is finalised to the worktree path and before `switch_session` is issued, the cloned `.jsonl` is rebased onto `formatAgentSessionFilePath(plan.cwd, ...)` — host-side `fs.rename` (with `copyFile + unlink` cross-device fallback) for non-sandboxed sessions, container-side `sessionFileCopy + sessionFileDelete` for sandboxed ones — and `plan.preExistingAgentSessionFile` plus the persisted `agentSessionFile` field are updated. Diagnose by comparing the clone's on-disk slug-dir against `slugify(plan.cwd)`; mismatch → rebase didn't fire. Regression test: `tests/e2e/continue-archived-worktree.spec.ts`.
- **Archived session footer shows wrong / default model on first load (e.g. `claude-opus-4-6` instead of the model the session actually ran on)** — the archived `auth_ok` branch in `src/server/ws/handler.ts` must push a `state` frame containing the persisted model immediately after `session_title`; otherwise the client's hardcoded placeholder default in `src/app/remote-agent.ts` (seeded into `_state.model` at construction time) leaks into the footer picker until a reconnect retriggers `get_state`. Live sessions don't show this because `getState()` is pushed proactively on connect; archived sessions need the symmetric push via `buildArchivedStateData()` (single source of truth shared with the `get_state` handler). Regression test: `tests/e2e/archived-footer-model.spec.ts`. See [docs/internals.md — Archived session WS handshake](docs/internals.md#archived-session-ws-handshake).
- **Stale draft resurrection** — `SessionStore.setDraft()` rejects writes with older `gen`; client must increment `gen` per change, tombstone carries highest.
- **400 "projectId required"** on `POST /api/goals|/api/sessions|/api/staff` — pass `projectId` or a `cwd` inside a registered project; no default fallback. See [docs/rest-api.md — Project resolution contract](docs/rest-api.md#project-resolution-contract).
- **Orphan remote branches / leaked `goal/`, `session/`, `goal-goal-*-<role>-*`, `staff-*`** — goal archive: `server.ts::deleteRemoteGoalBranches` snapshots agent branch names *before* `teamManager.teardownTeam` mutates `entry.agents` (otherwise per-role branches leak). Session archive: `session-manager.ts::terminateSession` fire-and-forgets `eagerDeleteRemoteSessionBranch` from `session-eager-branch-delete.ts` for non-delegate `session/*` branches that are fully merged into `origin/<primary>`; unmerged or non-`session/` branches defer to the 7-day `purgeOneSession` worktree cleanup. Staff: `cleanupWorktree(..., deleteBranch=true)` in `skills/git.ts` already push-deletes. Every push-delete (existing and new) is gated by `shouldSkipRemotePush()` so test mode (`BOBBIT_TEST_NO_PUSH=1`) never touches the remote. See [docs/design/orphan-remote-branch-cleanup.md](docs/design/orphan-remote-branch-cleanup.md) and [docs/debugging.md — Leaked remote branches](docs/debugging.md#leaked-remote-branches).
- **Image generation failure** — `POST /api/image-generation/generate` returns `400` for malformed input (`Missing prompt`, `prompt exceeds 8192 chars`, `n must be 1..4`) or `500 { error: "<provider message>" }` for any provider-side failure (missing credentials, Codex `n=1` clamp, `remote image exceeds 25 MB cap`, upstream HTTP error — never `[object Object]`). The endpoint never emits `502` or `503`. Codex driver model resolution: env var `BOBBIT_OPENAI_CODEX_IMAGE_DRIVER_MODEL` falls back through `gpt-5.5 → gpt-5 → gpt-4o`. Strict `unknown image model` validation lives on the WS `set_image_model` path, not the REST endpoint. See [docs/rest-api.md — Image generation](docs/rest-api.md#image-generation) for the full contract and [docs/internals.md — Image generation routing](docs/internals.md#image-generation-routing) for the data flow.
- **Tier 2.5 report missing / ffmpeg failed / `RECORDSCREEN=1` but no beats captured** — diagnostic order: (1) `tests/results/tier-2-5/report.html` only exists when at least one test wrote `<workerOutputDir>/beats.jsonl` AND the reporter ran. The reporter is registered in `tests/playwright-e2e.config.ts` only when `process.env.RECORDSCREEN === "1"` — without the env, no report file is produced regardless of `rec.capture()` calls. (2) If `report.html` exists but a specific test is missing: confirm that test imports from `tests/e2e/ui/fixtures.ts` (not `gateway-harness.ts`) and calls `await rec.capture(...)` at least once — `BeatRecorder` early-returns with zero filesystem activity when `RECORDSCREEN !== "1"`, and tests with no captured beats produce no video. (3) If ffmpeg is missing: the reporter resolves ffmpeg at runtime via `resolveFfmpeg()` in `tests/e2e/report/tier-2-5-reporter.ts` — `FFMPEG_PATH` env var first, then bare `ffmpeg` on `PATH`, both probed with `<bin> -version` (5s timeout). When neither resolves, the reporter logs `[tier-2-5] ffmpeg not found; videos and thumbnails skipped. Install ffmpeg (apt/brew/choco) or set FFMPEG_PATH=/abs/path/to/ffmpeg.` and still writes `report.html` with a top-level warning banner; per-test sections show "video missing". Install ffmpeg via `apt install ffmpeg` / `brew install ffmpeg` / `choco install ffmpeg`, or set `FFMPEG_PATH` to an absolute binary path. There is no `ffmpeg-static` npm dependency anymore (dropped so `npm install` succeeds offline — the postinstall fetched the binary from GitHub). See [docs/testing-tier-2-5.md](docs/testing-tier-2-5.md).
- **OAuth callback never completes** — user clicks "Connect" in Settings → Account, browser redirects, but the UI keeps showing the spinner. First check: poll `GET /api/oauth/flow-status?flowId=<id>&provider=<id>` directly (`curl -sk "$GW/api/oauth/flow-status?flowId=…&provider=anthropic"`). A `404 flow not found` either means the flow expired (check `expiresAt`) **or** the `provider` query param does not match the stored flow's provider — cross-provider polling is intentionally treated as not-found (defence in depth). If `complete: true` with `error: …`, the provider rejected the code. If `complete: false` past `expiresAt`, the browser-redirect handler never fired — check that `oauthStartExternal` constructed `flow.loginPromise` with the real promise (not a placeholder) and that `pendingFlows.set` happened **after** the auth URL was issued. See [docs/rest-api.md — OAuth](docs/rest-api.md#oauth).

## Git conventions

Primary branch is **`master`** (not `main`). Never create a `main` branch.

### Committing with a dirty working copy

When asked to commit, other unstaged/staged changes may exist from another agent or the user. **Never discard, stash, or reset those changes.** Stage only the files you modified (`git add <your-files>`) and commit. If your changes overlap and can't be cleanly separated, ask the user.

### Worktrees

Dev server runs from the **primary worktree** on `master`. All sessions use separate worktrees under `<project-root>-wt/<branch>/` (single-repo) or `<project-root>-wt/<branch>/<repo>/` (multi-repo). The default parent is `<rootPath>-wt/`; override with the project-level `worktree_root` field. Assistant sessions (goal/project/tool assistants) don't edit code and don't get worktrees.

**Branch namespaces:** `pool/_pool-<id>` (pool pre-builds, was `session/_pool-*` pre-Phase 3), `session/<id8>` (live regular sessions — produced immediately on pool claim, no first-prompt rename), `goal/<slug>-<id>` (goals), `staff-<name>-<id>` (staff). See [docs/dev-workflow.md — Worktree branch namespaces](docs/dev-workflow.md#worktree-branch-namespaces) and [docs/design/remove-session-worktree-rename.md](docs/design/remove-session-worktree-rename.md).

**Multi-repo invariant:** in multi-repo projects, every configured component repo (including data-only ones) gets a sibling worktree on the same branch. The agent's cwd is the per-branch container (`<rootPath>-wt/<branch>/`), mirroring the primary `rootPath` structure. See [docs/internals.md — Multi-repo & components](docs/internals.md#multi-repo--components) for the project model and [docs/internals.md — Session worktrees](docs/internals.md#session-worktrees) for layout and pool claim flow. The historical first-prompt rename has been removed — see [docs/design/remove-session-worktree-rename.md](docs/design/remove-session-worktree-rename.md).

**Always edit files in your session worktree, never in the primary worktree.** Editing the primary worktree risks merge conflicts during rebase, corruption of in-progress work by other agents, and breaking the running dev server. Even for infra files that need to end up in primary, the correct flow is: edit here → commit → push → pull from primary. See [docs/dev-workflow.md](docs/dev-workflow.md) for the full rationale.

**Pushing to remote `master` does NOT update the dev server.** Pull into the primary worktree:

```bash
cd <primary-worktree> && git pull origin master
```

You cannot `git checkout master` from a goal worktree — push to remote and pull from primary instead.

### Worktree setup command (per-component)

Configured **per component** via `components[*].worktree_setup_command` in `project.yaml`. Runs at the component's root path (`<worktree>/<component.repo>/<component.relative_path>`) with `SOURCE_REPO` env var pointing at the matching primary path. 2-minute timeout per component, non-fatal, `sh -c`. **No deduplication** — if multiple components in the same repo each define `worktree_setup_command`, it runs once per component.

```yaml
components:
  - name: myapp
    repo: "."
    worktree_setup_command: npm ci --prefer-offline --no-audit --no-fund
    # or: cp -r "$SOURCE_REPO/node_modules" node_modules
    commands: { build: npm run build, test: npm test }
```

Components without `worktree_setup_command` (including all data-only components) are silently skipped. Staff agent worktrees are auto-refreshed on each wake cycle — rebased onto primary and per-component setup re-run. `runComponentSetups()` exported from `src/server/skills/worktree-setup.ts`. Sandboxed staff skip host-side refresh.

**`components[*].worktreeSetupCommand` is the single source of truth.** Every worktree-creation site routes through `runComponentSetups()`: the pool's `_fill()` (single-repo and multi-repo), staff wake refresh, both `goal-manager.ts::setupWorktree` branches (single-repo and multi-repo), and `session-setup.ts::executeWorktreeAsync` (single-repo non-pool fallback). The legacy `setupCommand` parameter on `createWorktree` / `createWorktreeSet` and the `setupWorktreeDeps` helper have been removed — `runComponentSetups()` is the only path. The pool logs `[worktree-pool] running setup for components: <names>` on every fill so a silent regression — like the one where three call sites still read the migrated-away top-level `worktree_setup_command` and no-oped — is immediately visible. A regression-guard unit test (`tests/worktree-pool.test.ts`) fails if any source file outside `state-migration/migrate-project-yaml.ts` reads the legacy top-level key. See [docs/internals.md — Session worktrees](docs/internals.md#session-worktrees) and [docs/debugging.md — Worktree setup hook not running](docs/debugging.md#worktree-setup-hook-not-running).

## Reference docs

- [docs/internals.md](docs/internals.md) — tool policies, search, MCP, sandbox, config, disk state, goals, multi-project
- [docs/debugging.md](docs/debugging.md) — scannable debugging checklists
- [docs/dev-workflow.md](docs/dev-workflow.md) — running modes, restart workflow, worktree rationale
- [docs/testing-strategy.md](docs/testing-strategy.md) — test architecture and harness APIs
- [docs/testing-coverage.md](docs/testing-coverage.md) — per-area test coverage (prompt interactions, sidebar, sessions, resilience)
- [docs/goals-workflows-tasks.md](docs/goals-workflows-tasks.md) — goal/workflow/gate data model
- [docs/blocking-tools.md](docs/blocking-tools.md) — blocking-tool pattern (`verification_result`)
- [docs/non-blocking-ask.md](docs/non-blocking-ask.md) — non-blocking `ask_user_choices` flow
- [docs/design/unified-message-ordering-reducer.md](docs/design/unified-message-ordering-reducer.md) — unified message-ordering reducer (transcript ordering single source of truth)
