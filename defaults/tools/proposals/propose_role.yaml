name: propose_role
description: "Submit a role proposal for user review"
summary: "Propose a custom agent role with name, label, prompt, and optional fields."
provider:
  type: bobbit-extension
  extension: extension.ts
group: Proposals
docs: >-
  Parameters: name (required), label (required), prompt (required), tools, accessory.
