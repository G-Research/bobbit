name: propose_role
description: "Submit role proposal with name, label, prompt"
summary: "Propose a custom agent role"
params: [name, label, prompt, tools?, accessory?]
provider:
  type: bobbit-extension
  extension: extension.ts
group: Proposals
docs: >-
  Parameters: name (required), label (required), prompt (required), tools, accessory.
