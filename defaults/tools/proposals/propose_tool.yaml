name: propose_tool
description: "Submit a tool proposal for user review"
summary: "Propose a custom tool with tool name, action, and content."
provider:
  type: bobbit-extension
  extension: extension.ts
group: Proposals
docs: >-
  Parameters: tool (required), action (required), content (required).
