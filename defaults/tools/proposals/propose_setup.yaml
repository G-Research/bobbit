name: propose_setup
description: "Submit a project setup proposal for user review"
summary: "Propose project setup with action and optional command/model fields."
provider:
  type: bobbit-extension
  extension: extension.ts
group: Proposals
docs: >-
  Parameters: action (required), content, language, framework, testing,
  build_command, test_command, typecheck_command, test_unit_command,
  test_e2e_command, session_model, review_model, naming_model.
