name: propose_workflow
description: "Submit a workflow proposal for user review"
summary: "Propose a custom workflow with id, name, description, and gates."
provider:
  type: bobbit-extension
  extension: extension.ts
group: Proposals
docs: >-
  Parameters: id (required), name (required), description, gates.
