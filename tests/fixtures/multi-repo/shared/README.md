# shared (data-only)

This repo carries no `package.json` / commands. It exists to test the
data-only component pattern: a repo declared as a sibling worktree on
the goal branch but with no build/test commands of its own.
