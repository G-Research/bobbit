/**
 * Feature Y — a utility function that returns an activation status string.
 *
 * @returns A string indicating that Feature Y is active.
 */
export function featureY(): string {
  return 'Feature Y is active';
}
