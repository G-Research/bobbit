/**
 * Feature Y — returns a status string indicating the feature is active.
 *
 * @returns The string "Feature Y is active"
 */
export function featureY(): string {
  return 'Feature Y is active';
}
