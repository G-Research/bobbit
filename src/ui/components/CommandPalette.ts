// src/ui/components/CommandPalette.ts
// Retired: pack launchers now live in the shared session actions menu.

export const OPEN_COMMAND_PALETTE_EVENT = "bobbit-open-command-palette";

export function ensureCommandPalette(): undefined {
	return undefined;
}

export function openCommandPalette(): void {
	// Command palette launcher surface has been removed.
}
