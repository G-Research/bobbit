diff --git a/node_modules/@earendil-works/pi-coding-agent/dist/core/agent-session.js b/node_modules/@earendil-works/pi-coding-agent/dist/core/agent-session.js
index c99a78f..1895e4a 100644
--- a/node_modules/@earendil-works/pi-coding-agent/dist/core/agent-session.js
+++ b/node_modules/@earendil-works/pi-coding-agent/dist/core/agent-session.js
@@ -122,6 +122,9 @@ export class AgentSession {
     _extensionCommandContextActions;
     _extensionAbortHandler;
     _extensionShutdownHandler;
+    _toolResultGate;
+    _toolResultGateUpdateBytes = new Map();
+    _toolResultGateOverflow = new Set();
     _extensionErrorListener;
     _extensionErrorUnsubscriber;
     _modelRuntime;
@@ -234,26 +237,58 @@ export class AgentSession {
         };
         this.agent.afterToolCall = async ({ toolCall, args, result, isError }) => {
             const runner = this._extensionRunner;
+            let gatedResult = result;
+            let gatedIsError = isError;
+            const gate = this._toolResultGate;
+            if (gate) {
+                const overflowed = this._toolResultGateOverflow.delete(toolCall.id);
+                this._toolResultGateUpdateBytes.delete(toolCall.id);
+                if (overflowed) {
+                    return { content: [{ type: "text", text: "Tool result withheld by project result policy [ref: " + Math.random().toString(36).slice(2) + "]." }], details: undefined, usage: undefined, isError: true, replaceResult: true };
+                }
+                try {
+                    const gated = await gate({ toolCallId: toolCall.id, toolName: toolCall.name, input: args, result, isError });
+                    if (!gated || !Array.isArray(gated.content) || typeof gated.isError !== "boolean") throw new Error("Invalid result gate response");
+                    gatedResult = { content: gated.content, details: gated.details, usage: gated.usage };
+                    gatedIsError = gated.isError;
+                }
+                catch {
+                    return { content: [{ type: "text", text: "Tool result withheld by project result policy [ref: " + Math.random().toString(36).slice(2) + "]." }], details: undefined, usage: undefined, isError: true, replaceResult: true };
+                }
+            }
             if (!runner.hasHandlers("tool_result")) {
-                return undefined;
+                return gate ? { content: gatedResult.content, details: gatedResult.details, usage: gatedResult.usage, isError: gatedIsError, replaceResult: true } : undefined;
             }
             const hookResult = await runner.emitToolResult({
                 type: "tool_result",
                 toolName: toolCall.name,
                 toolCallId: toolCall.id,
                 input: args,
-                content: result.content,
-                details: result.details,
-                isError,
-                usage: result.usage,
+                content: gatedResult.content,
+                details: gatedResult.details,
+                isError: gatedIsError,
+                usage: gatedResult.usage,
             });
             if (!hookResult) {
-                return undefined;
+                return gate ? { content: gatedResult.content, details: gatedResult.details, usage: gatedResult.usage, isError: gatedIsError, replaceResult: true } : undefined;
+            }
+            if (gate) {
+                // Hook output is a patch over the already-gated value. Keep the
+                // core result replacement authoritative so nullish merge cannot
+                // restore the raw result's details or usage.
+                return {
+                    content: hookResult.content ?? gatedResult.content,
+                    details: hookResult.details ?? gatedResult.details,
+                    isError: hookResult.isError ?? gatedIsError,
+                    usage: hookResult.usage ?? gatedResult.usage,
+                    terminate: hookResult.terminate,
+                    replaceResult: true,
+                };
             }
             return {
                 content: hookResult.content,
                 details: hookResult.details,
-                isError: hookResult.isError ?? isError,
+                isError: hookResult.isError ?? gatedIsError,
                 usage: hookResult.usage,
             };
         };
@@ -347,6 +382,12 @@ export class AgentSession {
                 }
             }
         }
+        // While a result gate is installed, partial results are private input to
+        // the gate. Do not let the normal Session listeners observe raw chunks.
+        if (this._toolResultGate && event.type === "tool_execution_update") {
+            await this._emitExtensionEvent(event);
+            return;
+        }
         // Emit to extensions first
         await this._emitExtensionEvent(event);
         // Notify all listeners
@@ -494,6 +535,14 @@ export class AgentSession {
             await this._extensionRunner.emit(extensionEvent);
         }
         else if (event.type === "tool_execution_update") {
+            if (this._toolResultGate) {
+                // Do not inspect private chunks here. This method runs after
+                // ordinary same-realm extensions load, so using mutable
+                // serializers or collection helpers over raw updates would be
+                // an interception path. The core gate validates the complete
+                // terminal result before any normal fan-out resumes.
+                return;
+            }
             const extensionEvent = {
                 type: "tool_execution_update",
                 toolCallId: event.toolCallId,
@@ -504,6 +553,10 @@ export class AgentSession {
             await this._extensionRunner.emit(extensionEvent);
         }
         else if (event.type === "tool_execution_end") {
+            // The result has crossed the gate; never retain private raw update
+            // accounting after a terminal event (including error finalization).
+            this._toolResultGateUpdateBytes.delete(event.toolCallId);
+            this._toolResultGateOverflow.delete(event.toolCallId);
             const extensionEvent = {
                 type: "tool_execution_end",
                 toolCallId: event.toolCallId,
@@ -559,6 +612,8 @@ export class AgentSession {
             this.abortCompaction();
             this.abortBranchSummary();
             this.abortBash();
+            this._toolResultGateUpdateBytes.clear();
+            this._toolResultGateOverflow.clear();
             this.agent.abort();
         }
         catch {
@@ -1164,6 +1219,8 @@ export class AgentSession {
      */
     async abort() {
         this.abortRetry();
+        this._toolResultGateUpdateBytes.clear();
+        this._toolResultGateOverflow.clear();
         this.agent.abort();
         await this.waitForIdle();
     }
@@ -2028,6 +2085,16 @@ export class AgentSession {
             });
         this._baseToolDefinitions = new Map(Object.entries(baseToolDefinitions).map(([name, tool]) => [name, tool]));
         const extensionsResult = this._resourceLoader.getExtensions();
+        // Only the core extension loader can define this non-enumerable value
+        // before ordinary extension factories run. It is intentionally absent
+        // from ExtensionAPI/runtime bindings, so extension code cannot replace,
+        // clear, or observe the pre-fan-out result gate.
+        const coreToolResultGate = extensionsResult.runtime.__bobbitCoreToolResultGate;
+        if (coreToolResultGate) {
+            this._toolResultGate = coreToolResultGate;
+            this._toolResultGateUpdateBytes.clear();
+            this._toolResultGateOverflow.clear();
+        }
         if (options.flagValues) {
             for (const [name, value] of options.flagValues) {
                 extensionsResult.runtime.flagValues.set(name, value);
diff --git a/node_modules/@earendil-works/pi-coding-agent/dist/core/extensions/loader.js b/node_modules/@earendil-works/pi-coding-agent/dist/core/extensions/loader.js
index 099e74e..96d421d 100644
--- a/node_modules/@earendil-works/pi-coding-agent/dist/core/extensions/loader.js
+++ b/node_modules/@earendil-works/pi-coding-agent/dist/core/extensions/loader.js
@@ -54,6 +54,43 @@ const VIRTUAL_MODULES = {
     "@mariozechner/pi-coding-agent": _bundledPiCodingAgent,
 };
 const require = createRequire(import.meta.url);
+// This capability is deliberately loader-private. Bobbit configures its path
+// before Pi starts; the first extension load snapshots it and invokes the
+// factory before any ordinary extension has an opportunity to alter globals.
+// The closure is reused across reloads so an ordinary extension cannot change
+// process.env and install a replacement gate later.
+const defineCoreProperty = Object.defineProperty;
+let coreToolResultGateInitialized = false;
+let sealedCoreToolResultGate;
+let sealedCoreToolResultGateFailure;
+async function loadSealedCoreToolResultGate(cwd) {
+    if (coreToolResultGateInitialized) {
+        if (sealedCoreToolResultGateFailure)
+            throw sealedCoreToolResultGateFailure;
+        return sealedCoreToolResultGate;
+    }
+    coreToolResultGateInitialized = true;
+    try {
+        const configuredPath = process.env.BOBBIT_TOOL_RESULT_FILTER_GATE?.trim();
+        if (!configuredPath)
+            return undefined;
+        const resolvedPath = resolvePath(configuredPath, cwd, { normalizeUnicodeSpaces: true });
+        const factory = await loadExtensionModule(resolvedPath, undefined);
+        if (!factory)
+            throw new Error("Core tool-result gate does not export a factory");
+        const gate = await factory();
+        if (typeof gate !== "function")
+            throw new Error("Core tool-result gate factory returned an invalid gate");
+        // The wrapper keeps the gate private to the loader and prevents extension
+        // code from mutating a factory-owned property after activation.
+        sealedCoreToolResultGate = Object.freeze(async (event) => await gate(event));
+        return sealedCoreToolResultGate;
+    }
+    catch (error) {
+        sealedCoreToolResultGateFailure = error instanceof Error ? error : new Error(String(error));
+        throw sealedCoreToolResultGateFailure;
+    }
+}
 /**
  * Get aliases for jiti (used in Node.js/development mode).
  * In Bun binary mode, virtualModules is used instead.
@@ -400,6 +437,15 @@ async function loadExtensionsInternal(paths, cwd, eventBus, runtime, useCache =
     const resolvedCwd = cacheToken?.cwd ?? resolvePath(cwd);
     const resolvedEventBus = eventBus ?? createEventBus();
     const resolvedRuntime = runtime ?? createExtensionRuntime();
+    const coreToolResultGate = await loadSealedCoreToolResultGate(resolvedCwd);
+    if (coreToolResultGate) {
+        defineCoreProperty(resolvedRuntime, "__bobbitCoreToolResultGate", {
+            value: coreToolResultGate,
+            enumerable: false,
+            configurable: false,
+            writable: false,
+        });
+    }
     for (const extPath of paths) {
         const { extension, error } = await loadExtension(extPath, resolvedCwd, resolvedEventBus, resolvedRuntime, cacheToken);
         if (error) {
