diff --git a/node_modules/@earendil-works/pi-agent-core/dist/agent-loop.js b/node_modules/@earendil-works/pi-agent-core/dist/agent-loop.js
index 5b5da78..adad3ee 100644
--- a/node_modules/@earendil-works/pi-agent-core/dist/agent-loop.js
+++ b/node_modules/@earendil-works/pi-agent-core/dist/agent-loop.js
@@ -491,13 +491,36 @@ async function finalizeExecutedToolCall(currentContext, assistantMessage, prepar
                 context: currentContext,
             }, signal);
-            if (afterResult) {
+            // A cancelled result must never resume a pre-abort terminal value.
+            // The session gate receives this same signal and owns the opaque ref.
+            const gatedAfterResult = signal?.aborted
+                ? {
+                    content: [{ type: "text", text: "Tool result withheld by project result policy [ref: aborted]." }],
+                    details: undefined,
+                    usage: undefined,
+                    isError: true,
+                    replaceResult: true,
+                }
+                : afterResult;
+            if (gatedAfterResult) {
-                result = {
-                    ...result,
-                    content: afterResult.content ?? result.content,
-                    details: afterResult.details ?? result.details,
-                    usage: afterResult.usage ?? result.usage,
-                    terminate: afterResult.terminate ?? result.terminate,
-                };
-                isError = afterResult.isError ?? isError;
+                // A core-installed result gate is authoritative: unlike ordinary
+                // afterToolCall patches it must be able to remove details/usage.
+                if (gatedAfterResult.replaceResult === true) {
+                    result = {
+                        content: gatedAfterResult.content ?? [],
+                        details: gatedAfterResult.details,
+                        usage: gatedAfterResult.usage,
+                        terminate: gatedAfterResult.terminate,
+                    };
+                }
+                else {
+                    result = {
+                        ...result,
+                        content: gatedAfterResult.content ?? result.content,
+                        details: gatedAfterResult.details ?? result.details,
+                        usage: gatedAfterResult.usage ?? result.usage,
+                        terminate: gatedAfterResult.terminate ?? result.terminate,
+                    };
+                }
+                isError = gatedAfterResult.isError ?? isError;
             }
         }
diff --git a/node_modules/@earendil-works/pi-agent-core/dist/types.d.ts b/node_modules/@earendil-works/pi-agent-core/dist/types.d.ts
index 38009ad..5814291 100644
--- a/node_modules/@earendil-works/pi-agent-core/dist/types.d.ts
+++ b/node_modules/@earendil-works/pi-agent-core/dist/types.d.ts
@@ -55,6 +55,8 @@ export interface BeforeToolCallResult {
  * There is no deep merge for `content`, `details`, or `usage`.
  */
 export interface AfterToolCallResult {
+    /** Replace the complete result, including clearing details and usage. Reserved for core result gates. */
+    replaceResult?: boolean;
     content?: (TextContent | ImageContent)[];
     details?: unknown;
     isError?: boolean;
